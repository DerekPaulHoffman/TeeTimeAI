# Deployment Status

Last updated: 2026-07-23

## Live Vercel Deployment

- Project: `teetimeai`
- Production URL: `https://teetimespot.com`
- Alternate domain: `https://www.teetimespot.com`
- Previous Vercel domain: `https://teetimeai.vercel.app`
- Latest verified application release at the 2026-07-14 persistence-cleanup checkpoint: commit `cfed56d0753c3452840b6d502d9c80b5033fc705` at `teetimeai-au67ojszo-derekpaulhoffmans-projects.vercel.app`
- Application release deployment ID at that checkpoint: `dpl_8LcGV1YxH4XnZoyNj7eTRKZmdaW5`
- Deployment pointers in this file are timestamped audit evidence, not self-updating aliases. Verify the current production alias against the intended Git SHA with `npm run deployment:wait -- --sha <commit-sha>`.
- Vercel project ID: `prj_dI6LhLrDCSq06xgvtNvaKtF6Uz7Y`
- Vercel team/account ID: `team_qS5jqFYAovuxspGMzno0XtdK`

## Verified

- 2026-07-23 hourly product loop refreshed three stale public Course Guides from current official sources. Orchards now records its verified nine-hole layout plus current pro-shop and irrigation details; Short Beach retains its seasonal municipal par-three positioning, six-day reservation guidance, carts, and waterfront park context; The Tradition at Oak Lane retains its verified 18-hole, par-72 layout plus current range, bunker, and on-site dining details. All three profile drafts passed the protected dry run before the production upsert, and alert creation/provider monitoring state was unchanged.

- 2026-07-23 course-support repair added a reusable TeeItUp fallback for newer public booking pages whose booking-host alias no longer matches the provider API alias. When the legacy facility lookup returns 404, the adapter reads the signed-out booking page's bounded `alias` and `gnFacilityIds` bootstrap fields, keeps the official booking origin, uses the course's persisted timezone, and reads only the selected public tee-time inventory. It does not enter login, cart, reservation, or checkout.

- 2026-07-23 hourly product loop applied exact Google Place review `ChIJ99HILg3O54kRiJLIRU3WbfE` as `VERIFIED_PUBLIC` / `PUBLIC_GOLF_COURSE`. A real `[COURSE_LOOKUP_MISS]` for Wheeler Family Traditions reproduced because Google returned the playable course with `primaryType=sports_club` even though `types` also contained `golf_course`. The facility's current official site confirms a public 18-hole course at the same name and address. The exact live lookup changed from zero results to one, and the 15-mile Bethany-area discovery changed from 17 to 18 by adding Wheeler while preserving the existing nearby controls. The correction is database-backed and does not relax the conservative generic sports-club filter.

- 2026-07-22 hourly product loop applied exact Google Place review `ChIJM7da6psNyYcRZxvbI4C3ihM` as `VERIFIED_NON_COURSE` / `INDOOR_SIMULATOR`. The Creeks' current official site identifies The 19th Hole as its indoor Trackman simulator and practice facility, separate from the physical 18-hole course. Live ZIP `72761` discovery changed from four results to three by removing only that listing; Siloam Springs Golf Club, Creeks Golf Course, and Brush Creek Golf Course remained discoverable. The correction is database-backed and required no application deployment or broad filtering heuristic.

- 2026-07-16 correction: booking or transaction policy text is no longer a terminal monitoring disposition. Search checks no longer emit `BLOCKED_POLICY` from stored eligibility, Whoosh discovery no longer fetches terms, support closeout rejects legacy `AUTOMATION_PROHIBITED`, and legacy policy probes are reopened as adapter work. A normal signed-out Chrome session currently renders Gainfield Farms tee times, so TenFore discovery now keeps the official page and classifies the course as `NEEDS_REVIEW` instead of captcha-blocked. The consumer availability request still requires a reCAPTCHA token and a tokenless or headless request fails; TenFore's separate documented tee-sheet API requires a Bearer credential. Completing reusable Gainfield monitoring therefore requires official TenFore partner API access, not policy review or CAPTCHA bypass.

- 2026-07-16 course-support responder extended classification-only verification and independent terminal closeout proof to active exact Google Place reviews applied during an owned batch. A private or non-course review is accepted only when it is newer than the latest incident evidence and the persisted course is already reconciled to non-public, blocked state; stale, inactive, or contradictory reviews remain unresolved. Current official evidence identified the claimed engineering-only listing as a members-only synthetic private practice green, so an exact `VERIFIED_NON_COURSE` review now excludes it without tee-sheet access or external messaging. Verification passed 738 tests, lint with only the existing generated Workflow warning, production build, and the 84-test mixed-environment local UI smoke matrix.

- 2026-07-15 hourly product loop added reusable read-only Vermont Systems WebTrac monitoring for exact Navy `navyaims.com` golf-search links. Discovery learns the provider course code and conservative published booking window from the official surface. The adapter allowlists the HTTPS host and exact golf-search path, rejects redirects, reads only signed-out result rows, never calls cart or checkout, and sends golfers back to the official provider search to book. Casa Linda Oaks returned 42 public four-player slots in the focused live adapter check; its synthetic multi-cycle search completed successfully and the three-occurrence support incident resolved as `MONITORING_RESTORED` with zero alerts.

- 2026-07-15 hourly product loop added reusable GolfBack monitoring from exact official course links. Windsor Parke's official site and policy advertise real-time public tee times, and the provider's public API returned course configuration plus read-only availability to Tee Time Spot's declared fetch identity without login or a booking session. The adapter reads only course and tee-time endpoints, learns the provider's booking window, preserves source IDs, local times, player capacity, holes, and prices, and sends golfers to the exact official GolfBack course page to book.

- 2026-07-15 hourly product loop extended exact Google Place review reconciliation to `VERIFIED_PRIVATE` rows. Applying a reviewed private classification now marks an already-persisted course non-public and blocked, records verified intelligence, and resolves its support incident just as future discovery reads already exclude it. Exact review `ChIJAfI2SQDL5YgRBhnU_dStib0` classifies The Approach presented by the Eiras Family as a private Deerwood Country Club member amenity from the club's current membership, guest-policy, and golf-program evidence; nearby public Jacksonville controls remain discoverable.

- 2026-07-15 hourly product loop deployed commit `bc480b1` as Git deployment `dpl_7KUMPBd2XaE8223xH5iH53aCpmzQ`. Monitoring discovery now upgrades stale HTTP official-course URLs, with a bounded fallback for genuinely HTTP-only sites. Grassy Hill's HTTPS site exposed its legacy Club Prophet link, which redirected to a CPS booking page with public tee times but an explicit managed challenge on direct retrieval. The course is therefore preserved as direct public booking and blocked from automatic retrieval without bypassing the provider's access control; focused production recheck resolved its 29-occurrence support incident as `BLOCKED_POLICY`, preserved 79 supported-course matches, and sent no new alerts. Verification passed 503 tests, lint, build, local and production UI smoke (76/76 applicable checks), live route/API checks, and empty production error, warning, fatal, and 5xx log queries.

- 2026-07-15 hourly product loop recognized official TenFore booking links as direct public online booking with captcha-protected retrieval. Gainfield Farms' signed-out TenFore page visibly exposed tee-time availability, but the underlying request required an `x-recaptcha-token` and rejected a tokenless request. Discovery now preserves the official `fox.tenfore.golf` link, classifies automatic monitoring as `CAPTCHA_OR_QUEUE`, and never generates, replays, or bypasses the captcha token.

- 2026-07-15 hourly product loop corrected the public email preview to reflect the real five-course ranking limit. The status sample now keeps five representative monitoring outcomes instead of showing impossible priorities 6 and 7, while focused email tests retain coverage for the omitted before-window and insufficient-capacity variants.

- 2026-07-15 hourly product loop made the public `/search` route self-consistent across document, canonical, Open Graph, and Twitter metadata. Production evidence showed the route previously inherited the homepage social title and `og:url` because Next.js shallowly merges nested metadata; the route now uses the shared page-metadata helper with search-specific alert-setup copy and a focused regression test.

- 2026-07-15 hourly product loop hardened portfolio preparation so repeated authenticated-coverage gaps remain ranked with concrete unblock actions without terminally preempting the required evidence rotation. Coverage-only and otherwise healthy first passes now return nonterminal `exploration_required`; pending alerts, real-demand support incidents, actionable probes, and shippable feedback/browser/email/performance/metadata/security/research candidates keep their existing priority. Focused tests cover empty and healthy active queues, coverage-only ranking, and selection of a lower-priority shippable signal while the coverage gap remains visible.

- 2026-07-15 hourly product loop closes the bounded legacy residue left when default one-check synthetic scheduling shipped. A targeted migration completes only pre-deployment `TEST`/`AUTOMATION` searches that did not opt into multi-cycle mode and already have durable `CourseProbe` evidence; it does not touch `PUBLIC`/`UNCLASSIFIED` demand, explicit multi-cycle tests, or synthetic rows without check evidence. Pre-migration production readback found ten eligible TEST searches waiting as late as July 30 with five to seven probes each.

- 2026-07-14 hourly product loop prevented synthetic browser checks from creating owner-facing course-support work. `TEST` and `AUTOMATION` searches still record probes, matches, provider discoveries, and workflow evidence, but they no longer open or increment `CourseSupportIncident` rows. When a later synthetic check finds an existing unnotified incident with no active non-synthetic demand, it closes that incident as synthetic-only; a shared course incident remains open whenever real active demand exists. This bounds the 77-incident synthetic queue without hiding customer monitoring gaps or weakening adapter remediation.
- 2026-07-14 hourly product loop began consuming Google Places' February 2026 `googleMapsTypeLabel` field so an exact provider label of `Private Golf Course` is rejected even when the legacy primary type still says `golf_course`. The 99019 state-border corpus changed from 16 to 13 results by removing only CDA National Reserve Main Gate, The Golf Club At Black Rock, and CDA National Lake House. Liberty Lake, MeadowWood, Trailhead, Prairie Falls, The Highlands, Coeur d'Alene Public, Esmeralda, and the Coeur d'Alene resort remained in order; focused controls also preserve multi-layout Pinehurst and the verified-public Grassy Hill override. The change adds no broad private-name, club, address, proximity, or missing-field heuristic.
- 2026-07-14 hourly product loop separated synthetic saved demand from customer operations. `TeeSearch` now persists the same aggregate `PUBLIC`, `UNCLASSIFIED`, `AUTOMATION`, or `TEST` provenance already used by first-party funnel events, without adding a visitor/session identifier. A bounded browser header marks new saves; unknown values fail closed to `UNCLASSIFIED`. The additive migration backfills only searches whose following synthetic `search_submitted` event agrees on player count, selected-course count, and requested layout. Hourly active-demand counts, pending alerts, probes, and support incidents ignore `TEST`/`AUTOMATION`, while `automation:inspect` keeps synthetic searches and probes visible with their traffic class. Read-only pre-migration evidence found 30 exact TEST matches and one PUBLIC match across 31 recent active searches; a transaction-scoped production dry run classified 31 TEST and preserved seven older rows as UNCLASSIFIED, then rolled back cleanly.
- 2026-07-14 hourly product loop removed exact Google Place `ChIJa52pBnx754gRry26du_jGzo` (`BagBoyz2`) from course discovery after current Place Details showed a golf-typed record with no phone, website, rating, or independent course identity at 401 Golfview Street, while the City of Orlando and Dubsdread's official surface identify the actual nearby public course at 549 West Par Street. The production `GooglePlaceReview` row is an exact `VERIFIED_NON_COURSE` correction rather than a broad name, address, proximity, or missing-field rule. Live 15-mile Orlando discovery changed from 19 to 18 results with only BagBoyz2 removed; Dubsdread and Winter Park remained exactly once, and focused controls preserve a public resort, a similarly named legitimate course, and the verified-public Grassy Hill override. The existing saved preference was preserved but marked non-public and blocked; its support incident resolved, and a focused five-course check returned BagBoyz2 `BLOCKED_POLICY`, preserved 37 supported-course matches, sent zero new alerts, and completed successfully.
- 2026-07-14 hourly product loop restored Winter Park Golf Course monitoring from its legacy municipal URL. Official-site discovery now follows an exact same-origin course-detail label or path after policy links and before generic booking links, shares fetched pages without sharing course-specific decisions, and preserves the similarly named Winter Park Pines sibling. The current City of Winter Park WP9 page supplied the existing reusable TeeItUp surface; a focused live check returned Winter Park `NO_MATCH`, preserved 37 matches across supported sibling courses, created zero alerts, and resolved the incident as `MONITORING_RESTORED`.
- 2026-07-14 hourly product loop learned Short Beach Golf Course's reusable ForeUp metadata from the Town of Stratford's current official page. Monitoring discovery now decodes bounded escaped page state, recognizes Apptegy content-button `link` fields as well as embedded navigation `url` fields, and no longer turns escaped absolute URLs into malformed same-origin pseudo-links. Focused controls preserve ordinary HTML booking links, TeeItUp, TeeSnap, Whoosh policy classification, and private-course evidence.
- 2026-07-15 correction: auxiliary FAQ or registration wording no longer proves that a Whoosh booking surface hides availability. Account-required classification now needs gating evidence from the booking surface itself. Yale University Golf Course keeps its official Whoosh link and is shown as online-bookable with automatic monitoring unconfirmed; focused controls still block a Whoosh surface that actually requires sign-in to view availability.
- 2026-07-14 hourly product loop added privacy-safe funnel milestones at the two previously invisible onboarding boundaries: the first valid course selection and the signed-out alert CTA. Events contain only selected-course count, players, optional physical-layout preference, sanitized page/discovery-source buckets, and aggregate traffic class; they contain no course, location, email, account, referrer, visitor, or session identity. A marked rural `98237` production rotation broadened from zero courses at 15 miles to Skagit Preserve and Overlook at 30 miles on desktop and 327px mobile, opened the real Clerk sign-in modal, captured clean screenshots, produced no browser failures or overflow, and persisted no customer demand. Focused schema tests and four-viewport onboarding tests cover the event contract and ensure signed-out clicks still cannot submit an alert search.
- 2026-07-14 hourly product loop restored native keyboard submission to the course-search controls after a marked production desktop check showed that pressing Enter in the focused location field made no geocode or discovery request. The filter toolbar is now a semantic form whose default submit button uses the existing bounded discovery handler, so Enter and Search share validation, radius, analytics, and no-result behavior. A rural `84728` rotation produced clean 15- and 30-mile zero-result responses on desktop and 320px mobile with `AUTOMATION` traffic, no persisted demand, no overflow, and no browser failures. Focused Playwright passed 4/4, the full UI smoke passed 60/60, Vitest passed 75 files / 417 tests, lint had zero errors and one existing generated warning, and the production build completed successfully.
- 2026-07-14 hourly product loop hardened optional Clerk startup after a marked local browser check reproduced a full-page `Publishable key not valid` runtime overlay from copied development values. Clerk account mode now requires a decodable publishable key whose embedded frontend API has Clerk's documented shape, requires the public and secret keys to target the same environment type, normalizes both server environment values before `clerkMiddleware` initializes, and passes the normalized public key explicitly to `ClerkProvider`; malformed or copied placeholder configuration therefore renders Tee Time Spot's safe signed-out/setup state instead of crashing public browsing. Current Clerk environment-variable guidance and the installed SDK parser informed the focused validation contract.
- 2026-07-14 hourly product loop resolved the New London Golf Course support incident after its official Great Neck Country Club surface identified the facility as a private 18-hole course open to members and their guests. Browser discovery now recognizes the same explicit private-course/member-and-guest evidence shape it already handled for private clubs, while focused public-course controls prevent membership benefits or private-event copy from causing exclusion. A reviewed `VERIFIED_PRIVATE` Google Place correction removed only `ChIJ_YwBQd8P5okRc-utPXs6lls` from the live 15-mile Waterford discovery response (11 results to 10), preserving Shennecossett, Cedar Ridge, Goose Run, Fenwick, Pequot, Fox Hopyard, Lake of Isles, and the remaining provider response. A focused search check classified New London/Great Neck as `BLOCKED_POLICY`, preserved supported Cedar Ridge and Shennecossett checks, produced zero pending alerts, and closed the incident as direct-booking classified.
- 2026-07-14 focused persistence-cleanup release deployed Git commit `cfed56d0753c3452840b6d502d9c80b5033fc705` as `dpl_8LcGV1YxH4XnZoyNj7eTRKZmdaW5` (`teetimeai-au67ojszo-derekpaulhoffmans-projects.vercel.app`) after applying migration `20260714180000_add_google_place_reviews` with the direct Neon connection. The release moved 30 unique Google Place access/identity/alias reviews from runtime registries into `GooglePlaceReview`, retained generic filtering in code, added the protected dry-run-first `automation:place-review` operator command, removed the Woodhaven runtime layout fallback, preserved Woodhaven's verified nine-hole `Course` evidence, added the requested review/history indexes, and made hosted database/provider failures fail closed. The final migration was first applied and read back on an isolated Neon branch, then removed; production readback found 30 active/distinct reviews, the expected access and alias counts, all eight migration indexes, and Woodhaven `[9]` evidence. Verification passed 73 test files / 405 tests, lint with zero errors and one existing generated warning, build, local and production Playwright smoke 60/60, exact Git deployment/alias verification, live verified-public/private/non-course/canonical-alias and ordinary-public controls, `automation:inspect`, core route checks, Git parity, and an empty error/fatal log query for the audited production window.
- 2026-07-13 SEO and AI-discovery foundation release deployed commit `f17d8ff` as `dpl_6szXxz5jXkacK2mgjrNRpyF6tv7f` (`teetimeai-r58i9l50b-derekpaulhoffmans-projects.vercel.app`). The release added entity-first homepage copy, canonical metadata and structured data, crawlable trust and methodology pages, a public-golf guide hub with three source-backed guides, expanded sitemap and robots coverage, webmaster-verification hooks, and aggregate privacy-safe AI/search discovery attribution. Verification passed 59 test files / 307 tests, lint with zero errors and only the existing generated Workflow route warning, a production build, local and production Playwright smoke 52/52 across four viewports, 16 live route checks, exact canonical/JSON-LD/sitemap/robots assertions, both production aliases, and empty error, fatal, and 5xx log queries for the checked deployment window.
- 2026-07-13 Vercel release hardening made the Git integration the normal production deployment authority, added native production-alias gates for lint and type checking, and added GitHub `Verify` plus an owner-only manual protected `Preview Smoke`. A real Git preview created Neon branch `preview/chore/vercel-release-hardening` (`br-wandering-night-ategggm2`) from the production database branch. Preview no longer receives production-only Clerk, Google Places, Resend, automation, or email-action credentials; its smoke uses deterministic discovery responses. The automation bypass is scoped only to the same-origin cookie bootstrap/smoke step, and the Vercel Toolbar is disabled for new pre-production deployments.
- 2026-07-13 production release `09841d8` hardened owner analytics and the hourly autonomous-improvement contract. Vercel deployment `dpl_BZoWhMqR9uaUvrW88bYA3XKN1Pva` reached `Ready` with `teetimespot.com` and `www.teetimespot.com` aliases. Web Analytics and Speed Insights are enabled; first-party events and feedback now carry an explicit public, automation, test, or unclassified traffic class without adding visitor/session identifiers; analytics URLs are query-free; and production email actions use a dedicated encrypted `EMAIL_ACTION_SECRET`.
- The additive website-traffic migration was applied, then the explicitly approved historical privacy scrub removed query/hash data from 121 event-page values and removed one persisted search identifier from event metadata. Aggregate verification found no remaining affected event pages, feedback pages, or event metadata search identifiers.
- Phoenix discovery now deterministically keeps the canonical Arizona Grand and Ahwatukee public-course records while suppressing their known aliases. Live 30-mile discovery returned one canonical result for each course; the unrelated generic Tempe result remains intentionally unresolved.
- Release verification passed 277 unit tests, lint with zero errors, a production build, 24/24 local Playwright checks, 24/24 production Playwright checks, route and analytics-script HTTP checks, clean production error/4xx/5xx log groups, and `git diff --check`.
- `npm run test:run`
- `npm run lint`
- `npm run build`
- `npm run ui:smoke`
- Production Vercel deploy completed successfully.
- `teetimespot.com` was purchased through Vercel, attached to the `teetimeai` project, and verified.
- `www.teetimespot.com` was attached to the same project and verified.
- Clerk production instance exists for `teetimespot.com`; DNS, SSL, and Clerk mail DNS are complete.
- Resend free marketplace resource `teetimespot-alerts` is connected to Vercel. The `teetimespot.com` sending domain is verified with sending enabled.
- Resend production and development env vars include `RESEND_API_KEY`, `RESEND_EMAIL_DOMAIN`, and `ALERT_EMAIL_FROM`; Preview intentionally has no Resend credentials.
- Resend smoke sent successfully to `delivered@resend.dev` using a restricted sending key and fixed idempotency key.
- Neon Postgres marketplace resource is connected, migrated, and seeded with the ForeUP demo adapter data.
- Live `/` returns 200 and renders the Tee Time Spot intake.
- Live `/dashboard` returns 200. Signed-out users see the account-management prompt instead of a missing sign-in route.
- Live `/api/location/geocode?q=Trumbull%2C%20CT` returns 200 with Google Places text-search coordinates and `demo=false`.
- Live `/api/courses/discover?latitude=41.242&longitude=-73.209&radiusMeters=30000` returns 200 with 20 live Google Places courses and `demo=false`.
- Live `/api/courses/photo` returns a 302 redirect to a Google-hosted course image when Places returns a photo reference.
- Live `POST /api/searches` requires an authenticated account, uses that account's primary email, and accepts 1 to 5 ranked courses.
- Live `/api/automation/active-searches` returns 200 with the configured `x-automation-key`; latest smoke saw 9 active searches in the queue.
- Playwright browser smoke verified desktop course discovery, ranking, email-alert save, dashboard rendering, mobile layout, and zero browser console errors.
- Vercel runtime log scan found no errors after the final deployment; latest entries were 200/201 info logs for `/`, `/api/automation/active-searches`, and `/api/searches`.
- 2026-07-08 hourly product loop: repaired 3 active preferences that pointed at an unsupported duplicate Tashua Knolls row, then reran `npm run automation:poll`; latest run processed 8 active searches with 10 `NO_MATCH` probes and 0 `NEEDS_ADAPTER` probes.
- 2026-07-08 live smoke found Google Places returning 502 because the production API key value included a leading BOM character; Google Places clients now normalize copied env key values before sending headers or photo requests.
- 2026-07-08 production deploy `dpl_AGjhKBkqSAaxBaRK5eabKDRsAzkt` verified `/` 200, live Google Places geocode/discovery, and `POST /api/searches` with stale Tashua place ID resolving to the seeded `FOREUP`/`ALLOWED` course.
- 2026-07-08 production deploy `dpl_5jp4DbpdFWSAVizS21aFxxyBbdjZ` verified `/` 200, `/dashboard` 200, live Places text geocoding, live Places course discovery, photo proxy redirect, automation auth, and clean Vercel error logs.
- 2026-07-08 hourly product loop added a Postgres advisory lease around `npm run automation:poll` to prevent overlapping pollers from racing pending match alerts. Verification run `cmrco7fz30000iw15h1er63hn` processed 9 active searches and wrote 11 `NO_MATCH` probes with no adapter or fetch failures.
- 2026-07-08 hourly product loop found the local automation shell did not export `DATABASE_URL` or `AUTOMATION_API_KEY`, and found the session-level advisory lock could be orphaned through the pooled Neon/Prisma client. Automation scripts now load ignored local env files without overwriting existing process env, `npm run automation:inspect` prints redacted queue/probe/match health, and the poll lease uses `pg_try_advisory_xact_lock` with a rotated key. Verification run `cmrcqcj8z0000nw15dc3vxboz` processed 9 active searches and wrote 11 `NO_MATCH` probes; no pending alerts remained.
- 2026-07-08 hourly product loop refined `npm run automation:inspect` so `recentActionableProbes` reports only the latest unresolved probe state for each active search/course, while historical non-`NO_MATCH` events remain under `recentNotableProbes`. Verification run `cmrcsge260000z815f0hngog5` processed 9 active searches, latest inspect showed 0 current actionable probes and no pending alerts, and the intake default date now formats as a local calendar date instead of UTC.
- 2026-07-08 production deploy `dpl_BseBpUfu6Pc2gHHMqGFEdVntWcPT` verified `/` 200, `/dashboard` 200, live Places geocode, automation auth, and production Playwright intake rendering with the local-tomorrow date value and no failed resources.
- 2026-07-08 hourly product loop prevented repeat unsupported duplicate records for known supported courses by reusing nearby supported course rows when Google Places returns a composite facility name with overlapping meaningful name tokens. Repaired 1 active Tashua Knolls & Tashua Glen preference to the seeded ForeUP Tashua Knolls course, then reran `npm run automation:poll`; verification run `cmrcukqhy00003015bail0usx` processed 10 active searches, current actionable probes dropped to only Oak Hills Park Golf Course `NEEDS_ADAPTER`, and no pending alerts remained.
- 2026-07-08 production deploy `dpl_4MXwzjSdYrZvKv7hv2sm4cEQ7GKj` verified `/` 200, `/dashboard` 200, live Places geocode with `demo=false`, and live `POST /api/searches` with a composite Tashua Knolls & Tashua Glen course resolving to the seeded `FOREUP`/`ALLOWED` Tashua Knolls course. The smoke search was cancelled after verification, and a Vercel runtime log scan found no production warnings, errors, or fatals.
- 2026-07-08 hourly product loop fixed alert retry/drainage for previously created pending matches. The poller now drains active pending `TeeTimeMatch` alerts before deduping new tee-sheet results, dry-runs reserved seed/test recipient domains, normalizes copied Resend env values, and gives the Postgres advisory lease a 60-second transaction timeout for live tee-sheet polling. Verification run `cmrcws2c70000z41550ogsmzn` processed 10 active searches successfully after draining the stale demo pending alert; latest inspect showed `pendingAlerts: []` and the only current actionable probe remains Oak Hills Park Golf Course `NEEDS_ADAPTER`.
- 2026-07-09 hourly product loop identified Oak Hills Park Golf Course as an official ForeUP course (`booking/22739/11739`), updated the ForeUP adapter to support public tee sheets that do not require a booking class, seeded Oak Hills ForeUP metadata, and repaired the active Oak Hills course row in Postgres. Verification run `cmrcyxbi70000lc15iighvic0` processed 10 active searches; latest inspect showed `recentActionableProbes: []` and `pendingAlerts: []`.
- 2026-07-09 loop hardening added a committed Playwright UI smoke (`npm run ui:smoke`) covering desktop/mobile onboarding, typed-location discovery, 1-to-5 ranking limit enforcement, dashboard access states, same-origin failed requests, console/page errors, horizontal overflow, and too-small interactive targets. The first smoke found undersized clickable attribution/header links, which were fixed in CSS.
- 2026-07-09 production deploy `dpl_BqL96wEn4CKwSfc4zf5JP6NmfSYo` verified local tests/lint/build, local `npm run ui:smoke`, production `UI_SMOKE_BASE_URL=https://teetimespot.com npm run ui:smoke`, and clean Vercel error logs.
- 2026-07-09 hourly product loop found no current actionable probes and no pending alerts, so it shipped the strongest remaining UI/access gap: an accessible `/email-preview` route that renders the same alert HTML used by the Resend worker with fixed sample data and no email side effects. The Playwright smoke now covers the preview on desktop and mobile, and top-nav prefetching is disabled where it created noisy aborted same-origin RSC requests in production smoke.
- 2026-07-09 production deploy `dpl_ZbewQ6WNsULdWfhXah1VPz1DGnC9` verified `npm run test:run`, `npm run lint`, `npm run build`, local `npm run ui:smoke`, live `/email-preview` 200, production `$env:UI_SMOKE_BASE_URL="https://teetimespot.com"; npm run ui:smoke; Remove-Item Env:\UI_SMOKE_BASE_URL`, and no production warning/error/fatal Vercel runtime logs for the checked deployment window. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-09 hourly product loop found no current actionable probes or pending alerts, then fixed the strongest remaining intake UX/verification gap: unchanged saved searches now switch to a disabled `Search saved` state so users cannot accidentally submit duplicate demand from the same form. The UI smoke now stubs `/api/searches` in the onboarding test, verifies one successful save, and asserts unchanged saved demand is not submitted again.
- 2026-07-09 production deploy `dpl_6eXS4z9xyUdSdthUtPRueMxCD7eY` verified `npm run test:run`, `npm run lint`, `npm run build`, local `npm run ui:smoke`, live `/` 200, live `/email-preview` 200, production `$env:UI_SMOKE_BASE_URL="https://teetimespot.com"; npm run ui:smoke; Remove-Item Env:\UI_SMOKE_BASE_URL`, final `npm run automation:inspect` with `recentActionableProbes: []` and `pendingAlerts: []`, and Vercel runtime logs showing only 200/302 info entries for the smoke window. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-09 hourly product loop found no current actionable probes or pending alerts and baseline UI smoke passed, so it improved the strongest remaining onboarding UX gap: public homepage and intake copy no longer exposes internal `Codex`, `Postgres`, or `Clerk` implementation terms. The UI smoke now asserts the onboarding page stays free of those user-facing implementation details after saving a search.
- 2026-07-09 production deploy `dpl_7eu97b2T7FQzoTBXESt4PzxRpn6M` verified `npm run test:run`, `npm run lint`, `npm run build`, local `npm run ui:smoke`, live `/` 200, production `$env:UI_SMOKE_BASE_URL="https://teetimespot.com"; npm run ui:smoke; Remove-Item Env:\UI_SMOKE_BASE_URL`, final `npm run automation:inspect` with `recentActionableProbes: []` and `pendingAlerts: []`, and Vercel runtime logs showing only 200/302 info entries for the smoke window. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-09 hourly product loop found 8 active searches, no current actionable probes, no pending alerts, and a clean baseline UI smoke, so it extended the plain-language UI cleanup to the dashboard and save-error path. Public onboarding and dashboard smoke now assert that implementation terms stay out of user-facing copy while preserving clear setup, sign-in, and watchlist states.
- 2026-07-09 production deploy `dpl_Gm6YS7rVCjA47yhMs1nA9q5mpB6b` verified `npm run test:run`, `npm run lint`, `npm run build`, local `npm run ui:smoke`, live `/` 200, live `/dashboard` 200, live `/email-preview` 200, production `$env:UI_SMOKE_BASE_URL="https://teetimespot.com"; npm run ui:smoke; Remove-Item Env:\UI_SMOKE_BASE_URL`, final `npm run automation:inspect` with `recentActionableProbes: []` and `pendingAlerts: []`, and Vercel runtime logs showing only 200/302 info entries for the smoke window. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-09 hourly product loop found 8 active searches, no current actionable probes, no pending alerts, and a clean baseline UI smoke, so it tightened the player-count intake to one tee-time group. Shared validation now caps searches at 4 players, the intake renders only 1-4 player options, and UI/unit tests cover the limit.
- 2026-07-09 production deploy `dpl_5kEXtwwF14xvZDyEpAo9EVHDUPBE` verified focused validation tests, `npm run test:run`, `npm run lint`, `npm run build`, local `npm run ui:smoke`, live `/` 200, live `/dashboard` 200, live `/email-preview` 200, production `$env:UI_SMOKE_BASE_URL="https://teetimespot.com"; npm run ui:smoke; Remove-Item Env:\UI_SMOKE_BASE_URL`, final `npm run automation:inspect` with `recentActionableProbes: []` and `pendingAlerts: []`, and Vercel runtime logs showing only 200/302 info entries for the smoke window. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-09 hourly product loop found 8 active searches, no current actionable probes, no pending alerts, and a clean baseline UI smoke, so it strengthened the alert-only email surface. The email preview now states that Tee Time Spot never holds, reserves, pays for, or books tee times, and the rendered alert email tells golfers availability is first come, first served on the official course site.
- 2026-07-09 production deploy `dpl_EXaB4FkVYRaUBPUq8garqhuWVD86` verified focused email tests, `npm run test:run`, `npm run lint`, `npm run build`, local `npm run ui:smoke`, live `/` 200, live `/dashboard` 200, live `/email-preview` 200, production `$env:UI_SMOKE_BASE_URL="https://teetimespot.com"; npm run ui:smoke; Remove-Item Env:\UI_SMOKE_BASE_URL`, final `npm run automation:inspect` with 8 active searches, `recentActionableProbes: []`, `recentNotableProbes: []`, and `pendingAlerts: []`, and Vercel runtime logs showing only 200/302 info entries for the smoke window. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-09 hourly product loop found 8 active searches, no current actionable probes, no recent notable probes, no pending alerts, and a clean baseline UI smoke, so it tightened the alert-only UI boundary. Public homepage copy now avoids provider/adapter jargon, dashboard pending-match links say `Official site` instead of `Book`, and the smoke asserts these expectations.
- 2026-07-09 production deploy `dpl_CiKDSJXRtTBoJv4Ck65yDyGMC5TN` verified `npm run test:run`, `npm run lint`, `npm run build`, local `npm run ui:smoke`, live `/` 200, live `/dashboard` 200, live `/email-preview` 200, production `$env:UI_SMOKE_BASE_URL="https://teetimespot.com"; npm run ui:smoke; Remove-Item Env:\UI_SMOKE_BASE_URL`, final `npm run automation:inspect` with 8 active searches, `recentActionableProbes: []`, `recentNotableProbes: []`, and `pendingAlerts: []`, and Vercel runtime logs showing only 200/302 info entries for the smoke window. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-09 hourly product loop found 8 active searches, no current actionable probes, no recent notable probes, no pending alerts, and a clean baseline UI smoke, so it closed an intake validation gap. The save button now blocks same-day/past dates and end times that are not after the start time, shows specific guidance before submission, and the UI smoke asserts both guardrails.
- 2026-07-09 production deploy `dpl_BywstiMJyubQ5s5QXUkdrw31toQV` verified `npm run test:run`, `npm run lint`, `npm run build`, local `npm run ui:smoke`, live `/` 200, live `/dashboard` 200, live `/email-preview` 200, production `$env:UI_SMOKE_BASE_URL="https://teetimespot.com"; npm run ui:smoke; Remove-Item Env:\UI_SMOKE_BASE_URL`, final `npm run automation:inspect` with 8 active searches, `recentActionableProbes: []`, `recentNotableProbes: []`, and `pendingAlerts: []`, and Vercel runtime logs showing only 200/302 info entries for the smoke window. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-09 hourly product loop found 8 active searches, no current actionable probes, no recent notable probes, no pending alerts, and a clean baseline UI smoke, so it improved intake accessibility. Dynamic discovery/save messages now expose polite status regions, client-side errors expose alert regions, invalid date/time controls point at the guidance text, and the UI smoke asserts those accessibility contracts.
- 2026-07-09 production deploy `dpl_HGv1sMwdY94Yi6je1WCeZhTfxaRe` verified `npm run test:run`, `npm run lint`, `npm run build`, local `npm run ui:smoke`, live `/` 200, live `/dashboard` 200, live `/email-preview` 200, production `$env:UI_SMOKE_BASE_URL="https://teetimespot.com"; npm run ui:smoke; Remove-Item Env:\UI_SMOKE_BASE_URL`, final `npm run automation:inspect` with 8 active searches, `recentActionableProbes: []`, `recentNotableProbes: []`, and `pendingAlerts: []`, and Vercel runtime logs showing only 200/302 info entries for the smoke window. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-09 hourly product loop found 8 active searches, no current actionable probes, no recent notable probes, no pending alerts, and a clean baseline UI smoke. Current research confirmed transaction-scoped PostgreSQL advisory locks are the safer poller lease primitive for pooled Prisma/Neon connections, so the poller remains on `pg_try_advisory_xact_lock` with a 60-second transaction timeout instead of a session-level advisory lock. A one-off `npm run automation:poll` was run because the change touches poller lease behavior; verification run `cmrdkq4xt0000rc15kho2bpgp` processed 8 active searches, and the final inspect showed 22 recent `NO_MATCH` probes with `recentActionableProbes: []` and `pendingAlerts: []`. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-09 hourly product loop found the queue empty: `activeSearches: []`, `recentActionableProbes: []`, `recentNotableProbes: []`, and `pendingAlerts: []`; baseline UI smoke passed. The existing engagement/feedback and dashboard-management batch was carried through verification, with alert-only copy tightened so dashboard and email links say `Official site` / `Open official course page` instead of implying Tee Time Spot books tee times. Public analytics/feedback setup errors now avoid internal provider terms. Additive Prisma migrations for extra recipients and website engagement were checked; `migrate deploy` initially hit Prisma advisory lock `72707369`, a single stale idle pooled backend was terminated, and rerun reported no pending migrations. No one-off `npm run automation:poll` was run because no adapter or poller logic changed.
- 2026-07-09 production deploy `dpl_Go9Y5Cfrq4j4R1PJBDpaHpVNKZ7u` verified focused tests, `npm run test:run`, `npm run lint`, `npm run build`, local `npm run ui:smoke`, live `/`, `/dashboard`, and `/email-preview` 200, production `$env:UI_SMOKE_BASE_URL="https://teetimespot.com"; npm run ui:smoke; Remove-Item Env:\UI_SMOKE_BASE_URL`, final `npm run automation:inspect` with empty active queue and no pending alerts, and Vercel logs showing only 200/201/302 info entries for the smoke window. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-09 production deploy `dpl_AhXpEZKxiNfsBffmfc5bR33kHKkX` shipped the Figma Make-aligned product redesign plus extra alert email/dashboard visibility, engagement tracking, feedback capture, SEO metadata routes, and alert-email polish. Verified `npm run test:run`, `npm run lint`, `npm run build`, local `npm run ui:smoke`, live `/`, `/dashboard`, `/email-preview`, `/manifest.webmanifest`, `/robots.txt`, and `/sitemap.xml` 200, production `$env:UI_SMOKE_BASE_URL="https://teetimespot.com"; npm run ui:smoke; Remove-Item Env:\UI_SMOKE_BASE_URL`, `vercel inspect` showing `Ready` with `teetimespot.com` and `www.teetimespot.com` aliases, and Vercel error logs returned no errors for the checked window.
- 2026-07-09 hourly product loop found 1 active search, 4 current `NEEDS_ADAPTER` probes for unknown-platform courses, no pending alerts, and a suppressed duplicate Oak Hills ForeUP match. Baseline local UI smoke initially failed because Playwright reused a stale Node server on port 3000 whose CSS chunk returned 500; `playwright.config.ts` now defaults local smoke to port 3100 with `UI_SMOKE_PORT` override while production `UI_SMOKE_BASE_URL` behavior is unchanged. Production smoke then surfaced live `/dashboard` 500s from the prior deployment's Prisma runtime shape; production migrations were checked, the production-env dashboard query succeeded locally, and redeploying the current verified workspace restored dashboard access.
- 2026-07-09 production deploy `dpl_Foq73G4XvBAsWomGdEp9Av2wPpm2` verified `npm run test:run`, `npm run lint`, `npm run build`, local `npm run ui:smoke`, live `/`, `/dashboard`, and `/email-preview` 200, production `$env:UI_SMOKE_BASE_URL="https://teetimespot.com"; npm run ui:smoke; Remove-Item Env:\UI_SMOKE_BASE_URL`, final `npm run automation:inspect` with 1 active search, 4 current `NEEDS_ADAPTER` probes, and `pendingAlerts: []`, `vercel inspect` showing `Ready` with `teetimespot.com` and `www.teetimespot.com` aliases, and post-deploy Vercel error logs returning no error entries for the checked window. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-09 hourly product loop found 3 active searches, 4 current unknown-course `NEEDS_ADAPTER` probes, no pending alerts, and a clean baseline UI smoke. The browser-probe default now covers all five ranked courses, learned-metadata notes explicitly hand off to a poller rerun, and the probe run inspected Longshore, Oak Lane, Whitney Farms, and Fairchild Wheeler without entering checkout or learning reusable adapter metadata. Current research checked Google Maps JavaScript callback loading and Places Nearby Search rank preferences before carrying the discovery/UI batch through verification.
- 2026-07-09 production deploy `dpl_8PCikLTKYKk5t2u8r3yaFNwHDkV5` verified production migrations with `npx vercel env run -e production -- npx prisma migrate deploy` reporting no pending migrations, `npm run test:run`, `npm run lint`, `npm run build`, local `npm run ui:smoke`, live `/`, `/dashboard`, `/email-preview`, `/api/location/geocode`, and `/api/courses/discover` 200, production `$env:UI_SMOKE_BASE_URL="https://teetimespot.com"; npm run ui:smoke; Remove-Item Env:\UI_SMOKE_BASE_URL`, final `npm run automation:inspect` with 3 active searches, 4 current `NEEDS_ADAPTER` probes, and `pendingAlerts: []`, `vercel inspect` showing `Ready` with `teetimespot.com` and `www.teetimespot.com` aliases, and Vercel logs showing only 200/201/302 info entries for the smoke window. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-09 hourly product loop found 3 active searches, no current actionable probes, recent `MATCH_FOUND` probes across ForeUP, TEEITUP, and the CPS-backed custom Oak Lane path, and `pendingAlerts: []`. Candidate selected: finish and verify the adapter-learning batch so Longshore is ForeUP-backed, Whitney Farms and Fairchild Wheeler are TEEITUP-backed, Oak Lane is CPS-backed, and repeated browser-probe work decays into learning signals instead of being retried without new evidence. A one-off `npm run automation:poll` was run because adapter retrieval changed; verification run `cmrdtkfqj0000ok1517gmxl00` processed 3 active searches.
- 2026-07-09 production deploy `dpl_8hwQhexz3dj4Ugo7VNv2ew7XqUGM` (`teetimeai-b0dn8kx0f-derekpaulhoffmans-projects.vercel.app`) verified `npm run test:run` (19 files / 78 tests), `npm run lint`, `npm run build`, local `npm run ui:smoke`, live `/`, `/search`, `/dashboard`, and `/email-preview` 200, live geocode/discovery APIs 200, production `$env:UI_SMOKE_BASE_URL="https://teetimespot.com"; npm run ui:smoke; Remove-Item Env:\UI_SMOKE_BASE_URL` passing 6/6 on rerun after an initial transient discovery-click miss, final `npm run automation:inspect` with `recentActionableProbes: []` and `pendingAlerts: []`, `vercel inspect` showing `Ready` with `teetimespot.com` and `www.teetimespot.com` aliases, and Vercel logs showing only 200/201/302 info entries for the smoke window. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-09 hourly product loop found 3 active searches, no current actionable probes, recent `MATCH_FOUND` probes across all currently ranked course platforms, and `pendingAlerts: []`. Candidate selected: finish the current verified adapter/UI learning batch and repair the smoke harness drift that failed against the Leaflet fallback map. Current research checked Playwright webServer guidance; local smoke now uses the stable `3100` default port with `UI_SMOKE_PORT` override, and the smoke accepts Google Maps, fallback map, or explicit unavailable states without allowing a missing map area.
- 2026-07-09 production deploy `dpl_6PdXXPd9oKEti3hPqAd4AQQ6EEqY` (`teetimeai-32u6jd37a-derekpaulhoffmans-projects.vercel.app`) verified focused adapter/browser-discovery/improvement tests (18 tests), `npm run test:run` (19 files / 78 tests), `npm run lint`, `npm run build`, local `npm run ui:smoke`, `git diff --check`, live `/`, `/search`, `/dashboard`, `/email-preview`, geocode, and discovery 200 checks, production `$env:UI_SMOKE_BASE_URL="https://teetimespot.com"; npm run ui:smoke; Remove-Item Env:\UI_SMOKE_BASE_URL` passing 6/6, final `npm run automation:inspect` with 3 active searches, `recentActionableProbes: []`, and `pendingAlerts: []`, `vercel inspect` showing `Ready` with `teetimespot.com` and `www.teetimespot.com` aliases, and Vercel logs showing only 200/201/302 info entries for the smoke window. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-09 hourly product loop at 16:01 EDT found 3 active searches, all current ranked courses supported, `recentActionableProbes: []`, recent notable probes limited to historical `MATCH_FOUND`/old adapter-state evidence, and `pendingAlerts: []`. Local and production UI smoke both passed 6/6, live `/`, `/search`, `/dashboard`, `/email-preview`, geocode, and discovery returned 200, `vercel inspect` showed deployment `dpl_6PdXXPd9oKEti3hPqAd4AQQ6EEqY` still `Ready` with both production aliases, and Vercel logs showed no application errors. Outcome `no_op`: no one-off poll or code change was made because there was no current queue, adapter, pending-alert, provider, deployment, or UI smoke blocker. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-09 hourly product loop at 18:05 EDT found 1 active event-driven search with all five ranked courses supported, recent workflow runs succeeding, `recentActionableProbes: []`, and `pendingAlerts: []`. Local and production UI smoke both passed 6/6, live `/`, `/search`, `/dashboard`, `/email-preview`, geocode, and discovery returned 200, `vercel inspect` showed deployment `dpl_G5vwY6HS9KDBhCtKGqd7RRDCM4g4` Ready with `teetimespot.com` and `www.teetimespot.com` aliases, and Vercel logs showed only 200/201/302 entries for the smoke window. Outcome `no_op`: no one-off poll, deploy, or code change was made because there was no current queue, adapter, pending-alert, provider, deployment, or UI smoke blocker. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-09 hourly product loop at 19:05 EDT found 1 active event-driven search with all five ranked courses supported, latest workflow runs succeeding, 61 currently available matches, `recentActionableProbes: []`, and `pendingAlerts: []`. Local and production UI smoke both passed 6/6, live `/`, `/search`, `/dashboard`, `/email-preview`, geocode, and discovery returned 200, `git diff --check` found no whitespace errors, `vercel inspect` showed deployment `dpl_G5vwY6HS9KDBhCtKGqd7RRDCM4g4` Ready with `teetimespot.com` and `www.teetimespot.com` aliases, and Vercel logs showed only 200/201/302 info entries for the smoke window. Outcome `no_op`: no one-off poll, deploy, or code change was made because there was no current queue, adapter, pending-alert, provider, deployment, or UI smoke blocker. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-09 release verification added a 5-to-30-mile course discovery selector that defaults to 15 miles, enforces the same default and Google radius cap in the API, and verifies the exact default request in Playwright. The release also keeps the Figma-aligned dashboard/search polish, adds CPS and browser-discovery automation learning, fixes same-origin web-manifest paths, and moves the compact mobile feedback launcher beside rather than over form controls. Verification passed 20 test files / 81 tests, lint, production build, `git diff --check`, local Playwright smoke 6/6, desktop/mobile screenshot review, and a zero-warning production-mode local console check.
- 2026-07-09 hourly product loop at 20:06 EDT found 1 active event-driven search with all five ranked courses supported, latest workflow runs succeeding, 59 currently available matches, `recentActionableProbes: []`, and `pendingAlerts: []`. Local and production UI smoke both passed 6/6, live `/`, `/search`, `/dashboard`, `/email-preview`, geocode, and discovery returned 200, `git diff --check` found no whitespace errors, current Vercel Workflows docs still support the event-driven sleep/resume model, `vercel inspect` showed deployment `dpl_G5vwY6HS9KDBhCtKGqd7RRDCM4g4` Ready with `teetimespot.com` and `www.teetimespot.com` aliases, and Vercel logs showed only 200/201/302 info entries for the smoke window. Outcome `no_op`: no one-off poll, deploy, or code change was made because there was no current queue, adapter, pending-alert, provider, deployment, or UI smoke blocker. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-09 hourly product loop at 21:08 EDT found 1 active event-driven search with all five ranked courses supported, latest workflow runs succeeding, 54 currently available matches, `recentActionableProbes: []`, and `pendingAlerts: []`. Local and production UI smoke both passed 6/6, live `/`, `/search`, `/dashboard`, `/email-preview`, geocode, and discovery returned 200, `git diff --check` found no whitespace errors, current Vercel Workflows docs still support the event-driven sleep/resume model, `npx vercel inspect https://teetimespot.com` showed deployment `dpl_G5vwY6HS9KDBhCtKGqd7RRDCM4g4` Ready with `teetimespot.com` and `www.teetimespot.com` aliases, and Vercel logs showed only 200/201/302 info entries for the smoke window. Outcome `no_op`: no one-off poll, deploy, or code change was made because there was no current queue, adapter, pending-alert, provider, deployment, or UI smoke blocker. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-09 hourly product loop at 22:08 EDT found 1 active event-driven search with all five ranked courses supported, latest workflow runs succeeding, 54 currently available matches, `recentActionableProbes: []`, and `pendingAlerts: []`. Local and production UI smoke both passed 6/6, live `/`, `/search`, `/dashboard`, `/email-preview`, geocode, and discovery returned 200, `git diff --check` found no whitespace errors, current Vercel Workflows docs still support durable pause/resume with no maximum run or sleep duration, `npx vercel inspect https://teetimespot.com` showed deployment `dpl_G5vwY6HS9KDBhCtKGqd7RRDCM4g4` Ready with `teetimespot.com` and `www.teetimespot.com` aliases, and Vercel logs showed only 200/201/302 info entries for the smoke window. Outcome `no_op`: no one-off poll, deploy, or code change was made because there was no current queue, adapter, pending-alert, provider, deployment, or UI smoke blocker. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-10 hourly product loop at 00:33 EDT found 1 active event-driven search with all five ranked courses supported, recent workflow runs succeeding, `recentActionableProbes: []`, recent notable probes limited to successful `MATCH_FOUND` history, and `pendingAlerts: []`. Local and production UI smoke both passed 6/6, live `/`, `/search`, `/dashboard`, `/email-preview`, geocode, and discovery returned 200, `git diff --check` found no whitespace errors, current Vercel Workflows docs still support durable pause/resume for the event-driven scheduler, `npx vercel inspect https://teetimespot.com` showed deployment `dpl_74EmsqhMLRifQYgVgjbFxYgnhq3P` Ready with `teetimespot.com` and `www.teetimespot.com` aliases, and Vercel logs showed only 200/201/302 info entries for the smoke window. Outcome `no_op`: no one-off poll, deploy, or code change was made because there was no current queue, adapter, pending-alert, provider, deployment, or UI smoke blocker. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-10 hourly product loop at 01:37 EDT found 2 active event-driven searches, 5 current `NEEDS_ADAPTER` probes for one newly active five-course search, and `pendingAlerts: []`; local baseline UI smoke passed 6/6. Browser probing learned reusable adapter metadata for Timberlin Golf Course (`FOREUP`, `booking/22212/10124`) and Quarry Ridge Golf Course (`TEEITUP`, `quarry-ridge-golf-club`), then a focused adapter-verification run for search `cmrehuh8y000104l3wjk429dn` returned `NO_MATCH` for both newly supported courses with no new alerts. Fairview Farm Golf Course, Hunter Memorial Golf Course, and Stanley Golf Course SGC remain current `NEEDS_ADAPTER` gaps after browser inspection did not reveal reusable adapter metadata. The generic one-off `npm run automation:poll` processed 0 searches because the updated search is not due until its event-driven check window, so verification used the focused `runSearchCheck` path instead. Production smoke passed 6/6, live `/`, `/search`, `/dashboard`, `/email-preview`, geocode, and discovery returned 200, `npx vercel inspect https://teetimespot.com` showed deployment `dpl_74EmsqhMLRifQYgVgjbFxYgnhq3P` Ready with production aliases, Vercel logs showed only 200/201/302 info entries, and no deploy was needed because only database adapter metadata changed. Outcome `success`; remaining blocker is manual/official booking-surface research for the three unresolved courses. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-10 hourly product loop at 02:45 EDT found 2 active event-driven searches, 3 current `NEEDS_ADAPTER` probes, and `pendingAlerts: []`. Current-source research confirmed Hunter Memorial Golf Course's official tee-time page links to Teesnap and the public Teesnap tee sheet exposes course id `1210` through `/customer-api/teetimes-day`. Added a `CUSTOM`/`TEESNAP` adapter, recorded Hunter metadata in Postgres, and a focused `runSearchCheck` for `cmrehuh8y000104l3wjk429dn` returned `NO_MATCH` for Hunter, Timberlin, and Quarry Ridge while Fairview Farm Golf Course and Stanley Golf Course SGC remain `NEEDS_ADAPTER`. Deployed `dpl_Bzuzu2QR7tmdHwhDMZEKQRErn5ky` and verified focused tests, full tests, lint, build, local UI smoke, production UI smoke, live routes, and Vercel logs. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-10 hourly product loop at 03:40 EDT found 2 active event-driven searches, 2 current `NEEDS_ADAPTER` probes, and `pendingAlerts: []`; baseline local UI smoke passed 6/6. Current-source research found Fairview Farm's official site says to call the pro shop for tee times, while Stanley's official site offers online tee times through a Club Prophet widget. Browser discovery now decodes tee-time widget configs, learns CPS `baseURL` and course IDs from the official widget payload, and recorded Stanley Golf Course SGC as `CUSTOM`/`CPS` metadata (`stanleygolf`, `courseIds: [0]`). A focused `runSearchCheck` for `cmrehuh8y000104l3wjk429dn` returned `NO_MATCH` for Stanley, Hunter, Timberlin, and Quarry Ridge, while Fairview Farm Golf Course remains the only current `NEEDS_ADAPTER` course. Deployed `dpl_E8aCXoFK5myFYR9xTjdbTy4Utn5P` and verified focused tests, `npm run test:run` (22 files / 97 tests), `npm run lint` with only the existing generated Workflow route warning, `npm run build`, clean rerun local UI smoke, `git diff --check`, production UI smoke 6/6, live `/`, `/search`, `/dashboard`, `/email-preview`, geocode, and discovery 200 checks, `npx vercel inspect https://teetimespot.com` Ready with production aliases, final `npm run automation:inspect` with one current Fairview `NEEDS_ADAPTER` probe and `pendingAlerts: []`, and Vercel logs showing only 200/201/302 info entries. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-10 hourly product loop at 04:38 EDT found 2 active event-driven searches, one current Fairview Farm Golf Course adapter probe, and `pendingAlerts: []`; baseline local UI smoke passed 6/6. Current-source research confirmed Fairview Farm's official site and golf page list phone-only tee-time booking (`call 860.689.1000`) with no policy-safe public online tee-sheet surface, so the Fairview course row was marked `BLOCKED` with source-backed policy notes instead of leaving it as repeat adapter work. A focused `runSearchCheck` for `cmrehuh8y000104l3wjk429dn` returned `BLOCKED_POLICY` for Fairview and `NO_MATCH` for Timberlin, Hunter, Quarry Ridge, and Stanley, with no new alerts. No deploy was needed because the durable change was Postgres course state plus documentation. Verification passed `npm run test:run` (22 files / 97 tests), `npm run lint` with only the existing generated Workflow route warning, `npm run build`, local `npm run ui:smoke` 6/6, `git diff --check`, production `$env:UI_SMOKE_BASE_URL="https://teetimespot.com"; npm run ui:smoke; Remove-Item Env:\UI_SMOKE_BASE_URL` 6/6, live `/`, `/search`, `/dashboard`, `/email-preview`, geocode, and discovery 200 checks, `npx vercel inspect https://teetimespot.com` Ready with production aliases, final `npm run automation:inspect` showing `BLOCKED_POLICY` for Fairview and `pendingAlerts: []`, and Vercel logs showing only 200/201 info entries. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-10 hourly product loop at 05:36 EDT found 2 active event-driven searches, recent workflow checks succeeding, `pendingAlerts: []`, and a stale Fairview Farm `BLOCKED_POLICY` probe still being surfaced as current actionable work even though the course row is already `BLOCKED`. The inspect classifier and improvement snapshot now exclude blocked courses from `recentActionableProbes` while keeping those probes visible as notable history. Verification passed focused inspect-state tests, `npm run test:run` (23 files / 99 tests), `npm run lint` with only the existing generated Workflow route warning, `npm run build`, local `npm run ui:smoke` 6/6 after rerunning past a build/smoke race, `git diff --check`, final `npx vercel env run -- npm run automation:inspect` with `recentActionableProbes: []` and `pendingAlerts: []`, production `$env:UI_SMOKE_BASE_URL="https://teetimespot.com"; npm run ui:smoke; Remove-Item Env:\UI_SMOKE_BASE_URL` 6/6, live `/`, `/search`, `/dashboard`, `/email-preview`, geocode, and discovery 200 checks, `npx vercel inspect https://teetimespot.com` Ready on `dpl_E8aCXoFK5myFYR9xTjdbTy4Utn5P`, and Vercel logs showing only info-level 200 workflow entries. No deploy was needed because the change affects local automation-loop classification rather than the live user app. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-10 hourly product loop at 06:35 EDT found 2 active event-driven searches, recent workflow checks succeeding, `recentActionableProbes: []`, recent notable probes limited to successful `MATCH_FOUND` history, and `pendingAlerts: []`. Local and production UI smoke both passed 6/6, live `/`, `/search`, `/dashboard`, `/email-preview`, geocode, and discovery returned 200, `git diff --check` found no whitespace errors, current Vercel Workflows docs still support durable pause/resume with sleeps that do not consume compute, `npx vercel inspect https://teetimespot.com` showed `dpl_E8aCXoFK5myFYR9xTjdbTy4Utn5P` Ready with production aliases, and Vercel logs showed only info-level 200/201/302 entries for the smoke window. Outcome `no_op`: no one-off poll, deploy, code change, database change, or provider change was made because there was no current queue, adapter, pending-alert, provider, deployment, or UI smoke blocker. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-10 hourly product loop at 07:35 EDT found 2 active event-driven searches, recent workflow checks succeeding, `recentActionableProbes: []`, recent notable probes limited to successful `MATCH_FOUND` history, and `pendingAlerts: []`. Local and production UI smoke both passed 6/6, live `/`, `/search`, `/dashboard`, `/email-preview`, geocode, and discovery returned 200, `git diff --check` found no whitespace errors, current Vercel Workflows docs still support durable pause/resume with sleeps that do not consume compute and no maximum run/sleep duration, `npx vercel inspect https://teetimespot.com` showed `dpl_E8aCXoFK5myFYR9xTjdbTy4Utn5P` Ready with production aliases, and Vercel logs showed only info-level 200/201/302 entries for the smoke window. Outcome `no_op`: no one-off poll, deploy, code change, database change, or provider change was made because there was no current queue, adapter, pending-alert, provider, deployment, or UI smoke blocker. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-10 hourly product loop at 08:35 EDT found 2 active event-driven searches, recent workflow checks succeeding, `recentActionableProbes: []`, recent notable probes limited to successful `MATCH_FOUND` history, and `pendingAlerts: []`. Local and production UI smoke both passed 6/6, live `/`, `/search`, `/dashboard`, `/email-preview`, geocode, and discovery returned 200, `git diff --check` found no whitespace errors beyond existing CRLF conversion warnings, current Vercel Workflows docs still support durable pause/resume with sleeps from minutes to months, `npx vercel inspect https://teetimespot.com` showed `dpl_E8aCXoFK5myFYR9xTjdbTy4Utn5P` Ready with production aliases, and Vercel logs showed only info-level 200/201/302 entries for the smoke window. Outcome `no_op`: no one-off poll, deploy, code change, database change, or provider change was made because there was no current queue, adapter, pending-alert, provider, deployment, or UI smoke blocker. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-10 hourly product loop at 09:36 EDT found 2 active event-driven searches, recent workflow checks succeeding, `recentActionableProbes: []`, recent notable probes limited to successful `MATCH_FOUND` history, and `pendingAlerts: []`. Local and production UI smoke both passed 6/6, live `/`, `/search`, `/dashboard`, `/email-preview`, geocode, and discovery returned 200, `git diff --check` found no whitespace errors beyond existing CRLF conversion warnings, current Vercel Workflows docs still support durable pause/resume with sleeps from minutes to months, `npx vercel inspect https://teetimespot.com` showed `dpl_E8aCXoFK5myFYR9xTjdbTy4Utn5P` Ready with production aliases, and Vercel logs showed successful 200 workflow/page entries plus expected reserved-recipient `[email:dry-run]` warning logs. Outcome `no_op`: no one-off poll, deploy, code change, database change, or provider change was made because there was no current queue, adapter, pending-alert, provider, deployment, or UI smoke blocker. Checkpoints: `queue_confirmed=true`, `candidate_selected=true`, `tool_research_done=true`, `ui_smoke_done=true`, `verification_done=true`, `outcome_recorded=true`.
- 2026-07-10 notification release deployed `dpl_5C1v6g8vUZKb23C2qoHo1K2Qqy5c` with a one-time post-check setup report, at-most-daily course-status reports, and instant email reserved for newly available matches inside the saved date, time window, and player count. Course reports distinguish matches, outside-window inventory, insufficient capacity, dates not yet visible, adapter work, failed checks, and policy blocks, with official booking links and last-check time. The release also fixes UTC date handling in ForeUP and CPS, includes Teesnap adapter coverage, and adds the two-email preview. The additive status-email migration was applied before deployment. A focused production-data check of an active five-course search returned five successful `NO_MATCH` probes, no pending match alert, Resend status `sent` for the setup report, and a persisted five-course snapshot. Verification passed focused tests, `npm run test:run` (24 files / 105 tests), `npm run lint` with only the existing generated Workflow route warning, `npm run build`, local and production UI smoke 6/6, live `/`, `/dashboard`, and `/email-preview` 200 checks, `vercel inspect` Ready with all production aliases, and no production error or 4xx logs in the verification window.
- 2026-07-10 Discord community release deployed commit `5ddf30a` as `dpl_uCcB2nRqoBnUTJG8acXNr7AjNp2F`. The new Tee Time Spot Discord includes `welcome`, `announcements`, `general`, `feedback`, and `product-suggestions` channels plus the non-expiring invite `https://discord.gg/ThexF85xCd`; the site links to it from the global navigation, feedback panel, and footer with explicit feedback/product-suggestion copy. Verification passed `npm run test:run` (24 files / 106 tests), `npm run lint` with only the existing generated Workflow route warning, `npm run build`, local and production UI smoke 8/8, live `/`, `/dashboard`, and `/email-preview` 200 checks, exact live Discord-link readback, `npx vercel inspect https://teetimespot.com` Ready with `teetimespot.com` and `www.teetimespot.com` aliases, and no production error logs in the verification window.
- 2026-07-10 public-course discovery release deployed commit `0a1ab95` as `dpl_J3nwzCRqadvPonyM59aLK3G7u6yd`. Nearby discovery now combines primary-type constraints, generic ancillary/private membership classification, an IDs-only semantic public-course evidence query, and venue-aware logical deduplication instead of city-specific deny rules. Live San Francisco discovery within 24 miles returned 24 courses with Presidio Golf Course exactly once; The Olympic Club, Presidio Club Fitting, General Store, and Alameda Junior Golf Club were absent, while Fleming 9 Course and TPC Harding Park remained distinct. Verification passed `npm run test:run` (24 files / 108 tests), production build, committed production UI smoke 8/8, live `/`, `/dashboard`, `/email-preview`, `www.teetimespot.com`, and the SF discovery API with 200 responses, `vercel inspect` Ready with both production aliases, and no error-level or 5xx Vercel logs in the verification window.
- 2026-07-10 alert-experience release deployed commit `1b3f87a` as `dpl_8UzAEZ7SFH5RgqdVUniVUY5UHsSq`. The homepage, dashboard, ranked-course intake, and Discord community presentation were refreshed; dashboard alert cards now display the actual primary recipient email instead of “Just you” and retain the additional-recipient count. Verification passed `npm run test:run` (24 files / 108 tests), lint with only the existing generated Workflow route warning, `npm run build`, local and production UI smoke 8/8, live `/`, `/dashboard`, and `/email-preview` 200 checks, exact dashboard HTML readback showing the recipient email with no “Just you” copy, `vercel inspect` Ready with `teetimespot.com` and `www.teetimespot.com` aliases, and no production error or fatal logs in the verification window.
- 2026-07-10 recipient-privacy rollback promoted `dpl_J3nwzCRqadvPonyM59aLK3G7u6yd` back to production after the Clerk-disabled POC dashboard exposed another guest search owner’s full recipient email. The misattributed production search was removed, the affected guest’s original five-course search was restored with its prior window and an active workflow, and live dashboard readback confirmed the email address was no longer rendered. Keep the actual-recipient label limited to authenticated, owner-scoped dashboards before redeploying the alert-experience release; production Clerk remains gated by `CLERK_AUTH_READY=false`.
- 2026-07-10 authenticated-ownership hardening deployed commit `03ea9c7` as `dpl_2JnNfqbPc9VNPLJeawiJpWBXKR6R`. The dashboard no longer loads a global recent-search list when Clerk is unavailable; edit, delete, and check-now paths require account auth; authenticated users claim prior guest searches matching their normalized primary email; and user-scoped queries continue filtering by the database user id mapped from Clerk. Production remains safely gated because Vercel currently contains Clerk test/development keys and `CLERK_AUTH_READY=false`; replace them with live production-instance keys before enabling the flag. Verification passed 114 tests, lint with only the generated Workflow warning, build, local and production UI smoke 8/8, live owner-data redaction, 503 checks for disabled account APIs, production aliases, and a clean error-log scan.
- 2026-07-10 Figma mobile release deployed feature commit `106c3a6` plus guest-privacy guard `d043a7f` as `dpl_5XH5sRJBn3akF8h1hCyybRF7krZf` (`teetimeai-cikicp9im-derekpaulhoffmans-projects.vercel.app`). Homepage, search filters and course cards, mobile ranking controls, My Alerts cards, and email previews now match the Figma Make v36 phone layouts. Because production Clerk remains gated, the public dashboard now keeps recipient addresses and edit/delete controls private while authenticated dashboards remain owner-scoped. Verification passed `npm run test:run` (24 files / 108 tests), lint with only the existing generated Workflow route warning, `npm run build`, local and production UI smoke 8/8, live `/`, `/search`, `/dashboard`, `/email-preview`, and `www.teetimespot.com` 200 checks, exact dashboard readback with zero recipient email addresses, `vercel inspect` Ready with both production aliases, and no error, fatal, or 500 Vercel logs in the verification window.
- 2026-07-10 production authentication activation deployed commit `c9a4fcb` as `dpl_8PjrK5SrEyknrM4sg56M3AWEBc4c` (`teetimeai-asqtg3cbp-derekpaulhoffmans-projects.vercel.app`). Vercel production now uses the Clerk production instance's live keys with `CLERK_AUTH_READY=true`; email/password sign-up and sign-in are enabled, and the incomplete Google OAuth connection is disabled so users are not offered a broken provider. Live verification found `Sign in` in the global header, a working email/password modal with `Sign up`, a signed-out dashboard that exposes no alert-owner data, unauthenticated search APIs rejecting access, production UI smoke passing 8/8, `/`, `/dashboard`, and `/email-preview` returning 200, the deployment `Ready` with both production aliases, and no error-level Vercel logs in the checked window.
- 2026-07-10 account-owned alert creation deployed commit `c0e69b3` as `dpl_CyBYwD76mLrTJYk199KkqstBuJ3E` (`teetimeai-ioky25q1s-derekpaulhoffmans-projects.vercel.app`). Signed-out golfers can still discover and rank courses, but the alert button reads `Sign in to start sending alerts` and opens account access instead of submitting. `POST /api/searches` now requires an authenticated account, replaces any client-supplied primary email with the account email, and no longer creates guest owners. Verification passed 117 tests, lint with only the generated Workflow warning, production build, fresh local and production UI smoke 8/8, live `/`, `/search`, `/dashboard`, and `/email-preview` 200 checks, unauthenticated `GET` and `POST /api/searches` 401 checks, and `vercel inspect` Ready with both production aliases. The checked runtime error query contained only the intentionally generated 401 verification request and no 5xx failures.

- 2026-07-10 email availability and alert-control release deployed commit `ba264cd` as `dpl_GxphLjr8FTxqhUJC4tYJUQWBvvBh` (`teetimeai-mywqpxgqb-derekpaulhoffmans-projects.vercel.app`). Status and instant-alert emails now list every currently matching tee time by course with spots, price, and holes when available; new openings found together are batched into one email. Both email types include signed `I booked — stop these emails` and `Cancel this alert` controls that open a confirmation page before marking the search `COMPLETED` or `CANCELLED`, stopping its workflow schedule, and suppressing unsent matches. Verification passed 123 tests, lint with only the existing generated Workflow route warning, production build, local and production UI smoke 8/8, live `/`, `/dashboard`, `/email-preview`, `/alerts/stop?done=booked`, and `www.teetimespot.com` 200 checks, exact email-preview readback for both matching times and both stop controls, `vercel inspect` Ready with both production aliases, and no error-level Vercel logs in the verification window.

- 2026-07-10 Google sign-in was enabled for the Clerk production instance. Google Auth Platform now has an external, in-production `Tee Time Spot` web client using Clerk's exact production callback URI, and Clerk reports Google as used for sign-in with the standard `openid`, email, and profile scopes. Live Google OAuth completed successfully for the expected `derekpaulhoffman@gmail.com` account; the account panel exposes `Manage account` and `Sign out`, and the owner-scoped dashboard showed that same address on its single active alert. Signed-out verification also found the global `Sign in` control and a working `Continue with Google` option alongside email/password authentication. Production UI smoke passed 8/8 across desktop and mobile.
- 2026-07-10 public-course recall release deployed commit `99d9399` as `dpl_AxV63ReA6ACqnXeS2HovNoYMYyua` (`teetimeai-plzkkn66n-derekpaulhoffmans-projects.vercel.app`). Discovery now supplements typed Nearby results with bounded semantic public-course results when Google omits a valid course's golf type, enforces the requested radius locally, and no longer removes outdoor courses solely because of noisy secondary indoor or membership tags. Live 15-mile Trumbull discovery returned 15 courses with H Smith Richardson Golf Course and Chris Bargas Golf Club at Whitney Farms restored; live San Francisco discovery kept Presidio Golf Course to one result, excluded The Olympic Club, Presidio Club Fitting, General Store, and Alameda Junior Golf Club, and preserved Fleming 9 Course and TPC Harding Park separately. Verification passed 125 tests, lint with only the generated Workflow warning, production build, local and production UI smoke 8/8, direct desktop/mobile Trumbull rendering with zero console, page, or same-origin request errors, live core routes and both production aliases returning 200, `vercel inspect` Ready, and no error-level or 5xx Vercel logs in the verification window.
- 2026-07-10 dynamic course-pricing release deployed commit `5fbab2e` as `dpl_D9JgcUk7xx42you2Y8efDB12Mzx5` (`teetimeai-6ya63l5mf-derekpaulhoffmans-projects.vercel.app`). Supported adapter checks now preserve separate 9-hole and 18-hole price options, store recent official tee-sheet price summaries with course probes, and enrich nearby discovery from the last 30 days of observations or confirmed-match fallback data. Search cards replaced the hardcoded `18H · Par 72` label with evidence-backed price ranges or an explicit unavailable state; selecting a 9-hole or 18-hole price view sorts known prices first without hiding unknown-price courses. Verification passed 131 tests, lint with only the generated Workflow warning, production build, local and production UI smoke 8/8, live core routes and both production aliases returning 200, and live 15-mile Trumbull discovery retaining 15 courses including H Smith Richardson. The deployment is `Ready`; the runtime scan found one unrelated status-email idempotency-key mismatch on a successful Workflow check, with no 5xx response.

- 2026-07-10 missing-course lookup release deployed commit `a68cd23` as `dpl_3CKAsNBCwKe9GkYg2d2tEQZmkcZY` (`teetimeai-pji7xkmqv-derekpaulhoffmans-projects.vercel.app`). Golfers can search by course name and town when nearby discovery does not show the course, review public Google Places matches, and add or remove a verified place from the ranked alert list while monitoring support remains explicitly deferred until save. Verification passed 135 tests, lint with only the generated Workflow warning, production build, local and production UI smoke 8/8, live `/`, `/search`, `/dashboard`, `/email-preview`, and both production aliases with 200 responses, a real Bethpage Black lookup returning one course, `vercel inspect` Ready with both production aliases, and an empty production error-log query.

- 2026-07-10 course-list continuity release deployed commit `b09f120` as `dpl_4b83r3t6piuCpjKWGWPT9JTbiCK1` (`teetimeai-k8ga3nhc8-derekpaulhoffmans-projects.vercel.app`). Selecting a course no longer re-sorts nearby discovery results, so golfers can continue through the populated list while the separate selected-course panel preserves ranking. Verification passed 142 tests, lint with only the existing generated Workflow warning, production build, local and production UI smoke 8/8, live `/`, `/search`, `/dashboard`, `/email-preview`, and both production aliases with 200 responses, `vercel inspect` Ready with `teetimespot.com` and `www.teetimespot.com`, and an empty production error-log query.

## Current Runtime Mode

The public site is live as an email-alert proof of concept:

- Nearby discovery uses Google Places in production, defaults to a 15-mile radius with 5-to-30-mile choices, and falls back to demo course data only when `GOOGLE_PLACES_API_KEY` is absent.
- Google Places course thumbnails are implemented through `/api/courses/photo` and activate when live Places data returns `photos`.
- Google Places nearby search, text geocoding, and photo requests all use the same normalized API key value. The separate Google Geocoding API is not required for the current typed-location flow.
- Signed-out visitors may discover and rank courses, but alert creation and all management actions require an authenticated account.
- Search creation now reuses an existing supported nearby course row before creating a new course, so alternate demo/place IDs do not drop ForeUP adapter metadata.
- Clerk account mode is active in production with live keys and `CLERK_AUTH_READY=true`. Email/password and Google sign-up/sign-in are enabled with custom production Google OAuth credentials.
- Dashboard management actions require a signed-in Clerk account after Clerk auth is marked ready.
- Email sending is live through Resend when `RESEND_API_KEY` and `ALERT_EMAIL_FROM` are present. Local/automation runs still dry-run if those vars are absent.

## Provider Status and Remaining Setup

- Clerk production: account auth is complete and active. Domain, DNS, SSL, mail DNS, live keys, email/password authentication, Google OAuth, and the Vercel auth gate are configured.
- Google Maps key hygiene: Places API (New) is enabled and `GOOGLE_PLACES_API_KEY` is configured in Vercel. The current key was shared in chat during setup, so restrict it to the needed Google APIs and rotate it after confirming production remains healthy.
- Resend: core sending is configured. The original marketplace-managed API key cannot be deleted through the Resend API; manage/remove it from the Vercel Marketplace dashboard if a full cleanup is needed.
- Automation auth: `AUTOMATION_API_KEY` is set in production and local `.env.local`; keep it secret and rotate if exposed.
- Automation worker: local scripts load ignored env files automatically for operator runs, but hosted/scheduled workers should still provide env vars through the runtime rather than relying on checked-in files.
- Adapter coverage: Oak Hills Park Golf Course, Longshore Golf Course, and Timberlin Golf Course are configured as ForeUP from official booking links. Chris Bargas Golf Club at Whitney Farms, Fairchild Wheeler Golf Course, Richter Park Golf Course, The VUE CT, and Quarry Ridge Golf Course are configured through TEEITUP metadata. The Tradition Golf Club at Oak Lane and Stanley Golf Course SGC are configured through the CPS-backed custom adapter. Hunter Memorial Golf Course is configured through Teesnap metadata. Continue adding adapters only with policy-safe public retrieval evidence.

## Notes

- `npm run build` runs `prisma generate` before `next build` so clean Vercel builders have Prisma client types available.
- `.vercel/` and local env files are intentionally ignored. Do not commit provider credentials.
- The Codex loop remains alert-only and must not enter checkout, payment, verification-code, or account-specific booking flows.
- 2026-07-09 event-driven scheduling release: replaced the retired 15-minute production poller with one durable Vercel Workflow per active search, an authenticated daily recovery sweep, per-search Postgres leases/versioning, dashboard `Check now`, and availability state that remains visible after email delivery. Production migration `20260709203000_add_event_driven_search_checks` is applied; deployment `dpl_FpgsVFsGiJvAFEzF5z1Nr1n9HRFy` is `Ready` and aliased to `teetimespot.com` / `www.teetimespot.com`. Four prior test searches were deleted. A fresh random five-course search (`cmrdzl62x000104ibon6kvztc`) returned real availability from all five adapters (Oak Hills 20, Oak Lane 1, Whitney Farms 42, Longshore 1, H. Smith Richardson 3); a live per-request rerun used workflow `wrun_01KX4AWFW49JFT7DESPTGFC15J`, reconciled the total from 67 to 65 current matches with zero duplicate alerts, and scheduled its next check 15 minutes later. Verification: 85 tests, lint with only the generated Workflow route warning, build, local 6/6 UI smoke, production 6/6 UI smoke, production routes 200, and `automation:inspect` with no actionable probes or pending alerts.
- 2026-07-10 alert retry reliability release deployed commits `a386f41`, `611e991`, and `6220186` as `dpl_3BStuTZXzMomM5LfDdsEEt6Bb7Vn`. Resend idempotency keys now include the rendered email content, retry stop links stay stable for the search date, vanished unsent matches become `SUPPRESSED`, and timezone-less course slots are stored consistently as America/New_York instants across local and hosted runtimes. A focused production rerun recovered the affected active search, sent its newly available 8:00 AM Tashua Knolls alert, left zero pending alerts, and subsequent workflow checks completed successfully without duplicate sends. Verification passed 142 tests, lint with only the existing generated Workflow warning, production build, production UI smoke 8/8, `automation:inspect`, Ready aliases for `teetimespot.com` and `www.teetimespot.com`, and a clean recent Vercel error-log scan.
- 2026-07-10 global timezone release deployed commit `6348978` as `dpl_DW59KTkQeVcYL5BBJDqkd7mKizDo`. New searches store the golfer's browser timezone, every course stores an IANA timezone resolved from its coordinates, adapter-local times are converted with the selected course's timezone, CPS requests send course-specific timezone headers, and alert emails show course-local time plus the golfer's local time when different. Availability reconciliation now compares provider source id plus the exact normalized instant, retiring stale shifted duplicates. Production migration `20260711031500_add_search_and_course_timezones` is applied and all 20 existing courses were verified as `America/New_York`. A focused production recheck reduced the affected search from 110 available rows to 55 current rows without sending a new alert; final readback found 2 active searches, 56 available matches, zero matches outside their course-local date/window, and zero pending alerts. Verification passed 146 tests, lint with only the existing generated Workflow warning, build, local and production UI smoke 8/8, Ready production aliases, and an empty recent Vercel error-log query.
- 2026-07-11 email-cadence release deployed commit `fa40fbc` as `dpl_2pevSq8pv11LU53otr1R1pDp9u5U`. The first completed check now sends one setup/results email instead of a setup email plus a duplicate instant alert, morning status reports become due after 8:00 AM in the golfer's timezone, a coincident new-opening alert satisfies that morning's update, and a disappeared slot must remain gone for 30 minutes before it can alert again. Verification passed 182 tests, lint with only the existing generated Workflow warning, build, local UI smoke 10/10, production UI smoke 10/10, live `/`, `/search`, `/dashboard`, and `/email-preview` 200 checks, exact morning-copy readback, Ready production aliases, successful recent workflow runs with zero pending alerts, and an empty recent Vercel error-log scan.
- 2026-07-11 physical course-layout release deployed commit `e1fdb93` as `dpl_J3LtMgwL4Y8YkR5r9aTbf8QSzEfu`. Searches now persist an optional verified 9-hole or 18-hole physical-layout preference independently from purchasable round length, hide known incompatible courses while labeling unknown layout evidence, and stop legacy mismatches before adapter retrieval or email delivery. Woodhaven Golf Course is verified as a physical 9-hole course from its official site even though it sells 18-hole rounds. Production migration `20260711180000_add_course_layout_preferences` is applied. Verification passed 198 tests, lint with only the existing generated Workflow warning, build, local and production UI smoke 12/12, live Bethany discovery returning Woodhaven with `layoutHoleCounts: [9]`, live `/`, `/search`, `/dashboard`, and `/email-preview` 200 checks, Ready production aliases, successful workflow runs with zero pending alerts, and an empty recent Vercel error-log scan.
- 2026-07-11 missing-course discovery release deployed commit `f7dac13` as `dpl_8r8hbzQmtAaFFp8gbZ2143rDgeS5`. Evidence-backed public courses with ambiguous Google classifications now survive public-course filtering, direct course-name lookup performs a corroborating public-course query, and verified reported courses can be fetched by stable Google place ID when they fall inside the requested radius. Grassy Hill Country Club is registered from its official public-course evidence, and zero-result direct lookups create unresolved `[COURSE_LOOKUP_MISS]` feedback for the developer queue instead of implying the course does not exist. Verification passed 203 tests, lint with only the existing generated Workflow warning, build, local and committed production UI smoke 12/12, live `/`, `/dashboard`, and `/email-preview` 200 checks, Ready aliases for `teetimespot.com` and `www.teetimespot.com`, an empty deployment error-log scan, and live Milford discovery/UI readback showing 13 courses with Grassy Hill Country Club at 441 Clark Lane (2.7 miles away).
- 2026-07-11 search-window expiry release deployed commit `0110a80` as `dpl_TTBAHjayreV7VTmPzphhu9D9f9dW`. Durable workflows now stop and mark a search completed at the latest selected course's local requested end time instead of polling until calendar midnight; multi-time-zone searches use the latest absolute course cutoff. Verification passed 206 tests, lint with only the existing generated Workflow warning, build, local and production UI smoke 12/12, live `/`, `/search`, `/dashboard`, `/email-preview`, geocode, discovery, and both production aliases with 200 responses. Production search `cmrf5avh900001o15ro50xf6r` transitioned from repeated five-minute checks to `COMPLETED` / `STOPPED` with `nextCheckAt=null` and workflow outcome `completed`; no recent error, fatal, or 5xx logs were found.
- 2026-07-11 unsupported-probe deduplication release deployed commit `fb67417` as `dpl_4324AiYi4x56kDupMpFC2jbhfhYV`. Event-driven checks now retain one actionable `NEEDS_ADAPTER` observation until its outcome or message changes instead of writing the same unresolved course probe every five minutes. Verification passed 207 tests, lint with only the existing generated Workflow warning, build, focused probe tests 10/10, local and production UI smoke 12/12, live core routes/geocode/discovery and both production aliases with 200 responses, Ready deployment inspection, and an empty recent production error-log query.
- 2026-07-12 course-support incident release deployed commit `5fc7952` as `dpl_FWZQkErjzUJKC3HxddktdwvSBhbr`. Production migration `20260712150000_add_course_support_incidents` and sensitive `OPERATOR_ALERT_EMAIL` configuration are active. `NEEDS_ADAPTER` and `FETCH_FAILED` results now open persistent deduplicated owner incidents, escalate unresolved work after 30 minutes, stay actionable regardless of probe age, and send resolution emails after verified monitoring recovery or direct-booking classification; customer email copy claims the team was alerted only after confirmed delivery. Live Pequabuck verification opened incident `cmrhwuq8s0001qs15wd4v7x5d`, recorded the owner notification, prioritized its Chronogolf browser review, verified the provider's public profile reports online booking disabled, classified the course as `CONTACT_COURSE` / `BLOCKED` / `NO_ONLINE_BOOKING` at 0.95 confidence, and recorded both incident resolution and resolution notification. Verification passed 216 tests, lint with only the generated Workflow warning, build, local and production UI smoke 12/12, live `/`, `/dashboard`, and `/email-preview` 200 checks, Ready production aliases, zero open support incidents or pending alerts, a successful subsequent scheduled search check, and an empty recent production error-log query.
- 2026-07-12 generic-course deduplication release deployed commit `f6ddbe6` as `dpl_5CutMgpCAwJHUqEds6ZqNkp2k9Ad`. Course identity matching now removes the duplicate generic `Golf Course` result when it uniquely identifies a nearby named course while preserving ambiguous labels and distinct layouts at dense multi-course facilities. Verification passed 227 tests, lint with only the existing generated Workflow warning, build, local and production UI smoke 12/12, live `/`, `/dashboard`, `/email-preview`, and both production aliases with 200 responses, `vercel inspect` Ready with `teetimespot.com` and `www.teetimespot.com`, and an empty production error-log query.
- 2026-07-13 booking-window and autonomous adapter-remediation release deployed commit `4be4ef2` as `dpl_DGZ6xZRtLrd4oUB2yfH6ERyaVZqX`. Production migration `20260713191500_add_course_booking_windows` is applied. Supported adapters now learn provider-backed days-ahead and local release times, future workflows sleep until the next useful course release, and customer status email/dashboard copy explains when booking opens. Initial unsupported checks inspect official public surfaces before classification; unresolved incidents remain urgent autonomous adapter engineering work and cannot close as `needs_adapter` or notify the owner without structured evidence of concrete attempts plus one unavoidable external action. Production readback found the active Cape search with learned TeeItUp, Chronogolf, and custom-provider metadata, 12 confirmed Blue Rock matches already sent, zero pending alerts, and the remaining Dennis/Bayberry gaps queued for remediation. Verification passed 333 tests, lint with only the generated Workflow warning, build, local and production UI smoke 52/52, live `/`, `/search`, `/dashboard`, `/email-preview`, and booking-window guide 200 checks, Ready Git deployment on both canonical aliases, and empty recent error, fatal, and 5xx log queries.
- 2026-07-15 synthetic-lifecycle and Cleveland discovery release: additive migration `20260715040000_add_synthetic_multi_cycle` is applied. `TEST` and `AUTOMATION` searches complete after one successful workflow check unless creation explicitly opts into multi-cycle mode; failures still retry and customer demand remains unchanged. Production review `ChIJOQuc9RPtMIgR33aMCl2HdBA` removes only the Big Met parking point from discovery while preserving the real Big Met course. Focused verification passed 76 tests, the full suite passed 75 files / 436 tests, lint had zero errors plus the known generated warning, build succeeded, local UI smoke passed 60/60, and live `44113` desktop/mobile checks returned 19 ordered results without overflow or browser failures.
- 2026-07-15 browser-smoke observability release: deterministic direct-link fixtures now intercept their synthetic course-photo references instead of sending invalid photo requests to production, and the scenario fails on any future browser request, console, or page error. The triggering window contained 40 `/api/courses/photo` 400s and no warning, error, fatal, or 5xx rows; the post-release production smoke and log window must remain free of those fixture-generated 400s.
- 2026-07-15 course-knowledge release deployed commits `989db5e` and accessibility follow-up `515e6c0` as `dpl_BhPZ6cctrADrAPiGh2T8E2cmhRFp`. Additive migration `20260715160000_add_course_profiles` is applied; production contains 26 published Connecticut course profiles backed by 30 source records, with 8 qualifying Fairfield County courses and 7 New Haven County courses. Live verification passed 499 tests, lint with only the generated Workflow warning, production build, 72/72 applicable production browser checks across four viewports, all 26 sitemap course URLs, the three registered location hubs, course-to-search preselection, canonical and structured data checks, robots policy, both production aliases, and empty error, fatal, and 5xx log queries. The first production smoke identified undersized profile links on discovery cards; `515e6c0` increased those targets to 40 pixels before final verification.
- 2026-07-15 facility-first course-guide release deployed commits `a978834` and `26ec537` as Git deployment `dpl_BPjxB8HN1SpWAXvWLk2F5835U5j5`. Public course profiles now read as confident golfer-facing facility guides while claim mappings, evidence summaries, and source validation remain internal. Tashua Knolls now has a verified operational booking window of seven days at 5:30 a.m. course-local time backed by its official reservation policy; its published profile is content version 2 with the official booking reference included. Verification passed 85 test files / 508 tests, typecheck, lint with only the generated Workflow warning, build, 16/16 focused local course-page checks, and 76 applicable production browser checks across four viewports with 4 visual-only checks skipped by design. The canonical Tashua page, all three Connecticut hubs, sitemap, robots response, structured data, alert prefill, and core routes returned clean results, and recent production error, fatal, and 5xx queries were empty.
- 2026-07-15 Whoosh policy release: current official Whoosh terms are fetched for Whoosh booking surfaces, direct booking links remain available to golfers, and automated monitoring becomes `BLOCKED` / `AUTOMATION_PROHIBITED` only when the provider's explicit automated-search prohibition is verified. Yale's real-demand incident drove the reusable provider classification.
- 2026-07-15 Harmony identity correction: production review `ChIJV_YX1RG11okRxSmNMNmBRrY` classifies the exact Rochester listing as an `INDOOR_SIMULATOR`, matching its official site and the New York State Golf Association indoor-facilities directory. Applying a verified non-course review now also marks an already-persisted course non-public and blocked and resolves its support incident. Rochester discovery moved from 19 to 18 results with only Harmony removed; Genesee Valley, Durand Eastman, Shore Acres, Eagle Vale, and every other prior result remained. The focused synthetic search check recorded `BLOCKED_POLICY`, four supported physical-course controls returned `NO_MATCH`, and pending alerts remained zero.
- 2026-07-15 Hyde Park Chronogolf discovery release: official-site and browser evidence collection now recognizes `window.chronogolfSettings` and preserves bounded provider configuration before long marketing copy. Live Hyde Park discovery learned Chronogolf club `4006` and its exact course UUID; the existing read-only adapter observed 66 public player-eligible slots, matched 20 in the synthetic search window, resolved the engineering incident, and left zero pending alerts. Focused provider/discovery/search tests passed 70/70, full Vitest passed 545/545, lint had zero errors plus the known generated warning, build passed, and local UI smoke passed 84/84 before release.
- 2026-07-16 ForeUP discovery hardening: one-segment ForeUP booking roots no longer masquerade as schedule identifiers, targeted browser discovery is exact-course scoped and uses the distributed provider-request lease, and signed-out ForeUP access denials become reviewed direct-booking-only dispositions instead of endless adapter work. Westwoods Golf Course was reconciled to its official public 18-hole identity and ForeUP booking root; two bounded signed-out observations returned HTTP 403, so production now preserves the official booking link while classifying automatic access as blocked without attempting a bypass. Focused verification passed 93 tests, full Vitest passed 854/854, and lint plus the CI typecheck passed.
- 2026-07-21 TeeItUp metadata-recovery candidate: when a TeeItUp booking root lacks runnable metadata, course-support discovery now returns to the course-owned official site and requires a compatible page title or heading before trusting its provider link. Official course selectors are canonicalized while shared aliases, ambiguous facilities, unsafe URLs, unrelated course pages, already-runnable TeeItUp rows, and other provider families retain their existing safeguards. Pre-release verification passed 1,093 focused tests, all 2,194 Vitest tests, lint with only the existing generated Workflow warning, the production build, and 68 non-database UI checks; 16 local course-knowledge checks were unavailable because no local Postgres was running, so exact Git deployment, production UI smoke, and fresh Workflow evidence remain mandatory before responder closeout.
- 2026-07-21 legacy TeeItUp embed candidate: course-support discovery can now normalize a course-owned, public legacy `play.teeitup` embed into the existing canonical `book.teeitup` adapter metadata. The path accepts only a credential-free HTTPS provider root, requires one alias, one safe facility, and a compatible provider course name, and fails closed for ambiguous facilities, conflicting identities, unsafe URLs, nested hosts, or unrelated pages. No booking, account, checkout, CAPTCHA, queue, or provider availability surface was entered during implementation; a production Workflow must still prove a current runnable outcome after the exact Git deployment. Pre-release verification passed 1,098 focused tests, all 2,199 Vitest tests, lint with only the existing generated Workflow warning, CI typecheck, and the production build.
