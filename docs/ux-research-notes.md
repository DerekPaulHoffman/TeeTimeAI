# UX Research Notes

## 2026-07-22 - Monitor public Whoosh golf tee sheets without mixing in range inventory

- Current source check: Yale's signed-out Whoosh page and its public JSON response expose the club identity, a `GOLF_COURSE` facility, public visibility/bookability, booking-window days, tee-time capacity, permitted hole layouts, and customer prices. No sign-in, booking mutation, cart, payment, or checkout action is needed to read that availability.
- Non-course guard: Windy Hill's saved Whoosh URL targets `/agenda/driving-range/today` and presents Toptracer bay durations. The same club also exposes real Lake Course and Par 3 tee sheets. The agenda route may identify the club, but it is converted to the generic club landing and its simulator/range inventory must never be normalized as course availability.
- Decision: make WHOOSH a reusable runnable provider only with exact `app.whoosh.io/patron/club/<slug>` metadata. At read time, require a matching public club plus a visible, bookable facility whose provider type is exactly `GOLF_COURSE`; reject simulator, driving-range, and other facility types. Keep the official Whoosh club page as the customer booking link and stop before any transaction flow.

## 2026-07-21 - Keep public course guides available during evidence refresh

- Current evidence: production returned `404` for the Tashua Knolls course guide and Fairfield County hub while the Connecticut, New Haven County, and Cedar Ridge routes remained `200`. The stored Tashua profile still had complete official-source content and a public course identity, but a material course update had correctly moved its profile from `PUBLISHED` to `STALE`; public reads treated that refresh state as deletion. The same refresh stored Google's current `Greater Bridgeport Planning Region` administrative area instead of the legacy `Fairfield` county value, so a strict county-string query left the hub with four courses.
- Current practice: the official [Next.js `notFound` reference](https://nextjs.org/docs/app/api-reference/functions/not-found), updated February 27, 2026, documents that `notFound()` adds `noindex`, so a temporary refresh state was also removing an otherwise valid guide from search. Google Search Central's [crawl-error guidance](https://developers.google.com/search/docs/crawling-indexing/troubleshoot-crawling-errors), accessed July 21, 2026, says existing content should return its real page and status rather than an error page; its [sitemap guidance](https://developers.google.com/search/docs/crawling-indexing/sitemaps/overview), last updated December 10, 2025, treats sitemap URLs as the pages the site considers important.
- Geographic source: the Connecticut Secretary of the State's official [county-composition registry](https://portal.ct.gov/sots/register-manual/section-vi/counties---table-of-contents/state-marshals-by-county-and-composition-of-counties), revised November 14, 2025, lists Trumbull in Fairfield County and supplies the full municipality membership for both published county hubs. The Census Bureau's [2024 Connecticut county-equivalent file](https://tigerweb.geo.census.gov/tigerwebmain/Files/acs24/tigerweb_acs24_county_2024_bas24_ct.html) separately shows `Greater Bridgeport Planning Region` as the current county-equivalent label, explaining the provider-field drift.
- Decision: keep `STALE` profiles publicly readable and in the sitemap while their course remains public. Keep them in qualified location hubs only when the course is still public and automatically supported, and resolve Connecticut county hubs through either the legacy county value or the state's official municipality membership. This preserves the five-course publication threshold without treating an entire planning region as a legacy county. Non-public courses remain excluded, and the stale state still stays in the operator queue for evidence refresh.
- Rotation evidence: signed-out production ZIP `04730` returned Houlton Community Golf Course on desktop and 320px mobile with the `AUTOMATION` marker. Add/rank behavior, the actual mobile viewport, images, console state, and horizontal overflow were clean; no alert was saved. A distorted full-page mobile capture was rejected as a viewport-tool stitching artifact because the live viewport and measured element widths were correct.

## 2026-07-16 - Public visibility keeps monitoring work open

- Gainfield evidence: a normal signed-out Chrome session at [Gainfield's official TenFore booking page](https://fox.tenfore.golf/gainfieldfarms) currently renders dated tee times, capacity, holes, and prices. The reCAPTCHA badge does not by itself mean the page is unavailable to golfers.
- Technical boundary: the consumer `BookingEngineV4/booking-times` request includes `x-recaptcha-token`; the same request without that token returns HTTP 400, and a headless page receives the same failure. Tee Time Spot will not generate, solve, replay, or bypass that token.
- Supported integration path: [TenFore's API help](https://api.tenfore.golf/Help) documents read endpoints including `TeeSheet`, `TeeSheetExtV`, and `TeeSheetTimes`, while a credential-free request returns HTTP 401 with `WWW-Authenticate: Bearer`. [TenFore's official product page](https://tenfore.golf/) describes an open partner API and names Noteefy among its integrations.
- Decision: preserve Gainfield's official link, classify TenFore as open engineering work rather than a terminal captcha disposition, and request official partner API access before implementing a reusable adapter. Booking or transaction policy text never changes public read-only eligibility; only current technical access evidence may classify a real gate.
- Recurrence guard: Whoosh terms are no longer fetched during discovery, legacy `BLOCKED_POLICY` evidence is selected as adapter work, policy-only support closeout is rejected, and customer copy presents a public online legacy block as “Adding monitoring.”

## 2026-07-15 - Exact non-course reviews must reconcile saved course state

- Sources: [The Harmony Golf Club's official site](https://theharmonygolfclub.com/), accessed July 15, 2026, calls the Rochester venue a private indoor golf simulator at 274 N Goodman Street, Suite D308. The [New York State Golf Association indoor-facilities directory](https://nysga.org/competitions-where-to-play-indoor-facilities), accessed the same day, independently lists The Harmony Golf Club under Rochester indoor facilities.
- Tee Time Spot evidence: Google Places currently labels the exact Rochester place `ChIJV_YX1RG11okRxSmNMNmBRrY` as `primaryType: golf_course`, so it survived typed discovery, became ranked synthetic demand, and opened an adapter incident despite not being a physical course. Production Rochester discovery returned 19 results before review, including the simulator.
- Decision: keep exact evidence-backed non-course corrections in `GooglePlaceReview`, and make the dry-run-first operator command reconcile any already-persisted `Course` row to non-public, blocked state while resolving its support incident. Do not add a broad name, club, address, or suite-number heuristic from one example.
- Preservation: after the review, Rochester discovery returned 18 results; only Harmony disappeared. Genesee Valley, Durand Eastman, Shore Acres, Eagle Vale, resort/multi-layout results, and all other prior results stayed present. A focused search check recorded Harmony as `BLOCKED_POLICY`, retained four supported physical controls, sent no alert, and left zero pending alerts.
- Rotation evidence: signed-out production ZIP `87901` returned two New Mexico results on desktop and 320px mobile. Both sessions retained the `AUTOMATION` marker; screenshots, add/remove ranking, images, console/network state, and horizontal overflow were clean. The generic `Golf Course` result is a ranked follow-up requiring separate identity evidence.

## 2026-07-15 - Keep TenFore booking public without bypassing captcha protection

- Source: Gainfield Farms Golf Course's official site, accessed July 15, 2026, links `https://fox.tenfore.golf/gainfieldfarms` as “Book Tee Time.” The signed-out TenFore page visibly returned dated tee times, player capacity, hole counts, and online-booking cards without requiring an account.
- Safety evidence: the public page's availability request requires an `x-recaptcha-token`. A direct request without that browser-issued token returned HTTP 400. Tee Time Spot must not generate, replay, solve, or bypass captcha-protected retrieval even though the page itself is useful to golfers.
- Decision: recognize `fox.tenfore.golf/<tenant>` as a direct public booking surface, keep that official link instead of a later simulator page, and classify automatic monitoring as blocked with `CAPTCHA_OR_QUEUE`. This resolves support work honestly while preserving the golfer's ability to check and book direct.
- Rotation evidence: signed-out production ZIP `85621` returned Medici's Palo Duro Creek Golf on desktop and 320x800 mobile with the `AUTOMATION` marker. Screenshots, add/remove ranking, image checks, and horizontal-overflow metrics were clean; no alert was saved. One console exception without a source URL appeared only in the in-app browser after viewport tooling, while committed local smoke and production runtime logs remained clean.

## 2026-07-15 - Keep the public email preview inside the five-course contract

- Source: the official [React Email CLI documentation](https://react.email/docs/cli), accessed July 15, 2026, describes previewing templates locally while recommending the same prop-driven render utility used at send time. Tee Time Spot already follows that pattern, so the preview data should remain a valid product example rather than a separate fictional shape.
- Tee Time Spot evidence: the live `/email-preview` status report rendered priorities 1 through 7 even though the product and save API allow at most five ranked courses. The same renderer correctly displayed the outcomes, but priorities 6 and 7 could never appear in a customer email.
- Decision: keep five representative courses in the public status preview and add a browser regression proving that Priority 5 is present while priorities 6 through 9 are absent. Focused email unit tests continue to cover the omitted before-window and insufficient-capacity outcome variants.
- Rotation evidence: signed-out production ZIP `97814` returned Quail Ridge Golf Course on 1440x1000 desktop and 320x800 mobile. Add/rank behavior, screenshots, images, and horizontal-overflow checks were clean with `AUTOMATION` traffic and no saved demand. Quail Ridge's official site confirms public online reservations, but monitoring support requires separate provider and policy evidence.

## 2026-07-15 - Keep public search social metadata self-consistent

- Source: the [Next.js `generateMetadata` reference](https://nextjs.org/docs/app/api-reference/functions/generate-metadata), updated March 25, 2026, documents that metadata is shallowly merged by route segment and that a child page inherits the full parent `openGraph` object when it does not define one.
- Tee Time Spot evidence: production `/search` had the correct document title and canonical URL, but its Open Graph and Twitter title remained `Tee Time Spot | Public Golf Tee Time Alerts` and `og:url` remained the homepage. A shared high-intent search link therefore described a different page even though the repository already had a page-metadata helper used by the other public routes.
- Decision: build `/search` metadata through the shared helper so canonical, Open Graph, and Twitter fields all identify `https://teetimespot.com/search`, retain the shared 1200x630 image, and use search-specific alert-setup copy.
- Rotation evidence: signed-out production ZIP `96134` returned no courses at 15 miles and Indian Camp Golf Course at 30 miles on 1440x1000 desktop and 320x800 mobile. The marked `AUTOMATION` sessions showed a clear widen-search recovery, stable add/rank behavior, zero horizontal overflow, no broken images or same-origin HTTP failures, and no saved demand.

## 2026-07-15 - Distinguish direct online booking from automatic monitoring

- Evidence: public `BROKEN` feedback on Yale University Golf Course said the "Official site only" status was incorrect because the linked course flow supports booking. Production reproduction showed the card linking to Yale's official course site while using the generic manual-only label.
- Current source check: Yale's official [2026 course page](https://yalebulldogs.com/sports/2026/2/24/yale-golf-course.aspx), published February 24, 2026, links to Whoosh for registration and booking. A signed-out live check of that Whoosh page on July 15 showed the date picker, available tee times, remaining capacity, holes, and price before sign-in. Registration is needed to finish booking, but is not currently required to view availability. The July 22 reusable adapter review confirmed that the same signed-out response can be monitored while leaving booking on Whoosh.
- Accessibility guidance: W3C's [WCAG 2.2 Headings and Labels guidance](https://www.w3.org/WAI/WCAG22/Understanding/headings-and-labels), updated March 9, 2026, says labels should describe their purpose; a generic site-only label did not communicate the available booking action.
- Decision: classify blocked `PUBLIC_ONLINE` and `ONLINE_OR_PHONE` courses as `DIRECT_ONLINE`, present "Book online directly," and keep the explicit no-automatic-monitoring explanation. Preserve `OFFICIAL_SITE_ONLY` for blocked courses whose booking mode is unknown.

## 2026-07-15: Coverage blockers should inform exploration, not preempt it

- Sources: [Vercel Runtime Logs](https://vercel.com/docs/logs/runtime), published 2026-02 and accessed 2026-07-15 America/New_York, documents environment, status-code, and branch filters for separating production incidents from expected traffic. [Prisma aggregation and grouping](https://www.prisma.io/docs/orm/prisma-client/queries/aggregation-grouping-summarizing), accessed 2026-07-15, recommends filtering records before grouping so the database returns only the evidence needed for a summary.
- Tee Time Spot evidence: the v11 prepare handoff ranked six repeated Discord-history gaps and five reserved-dashboard-auth gaps, then returned terminal `needs_human` with `candidate_selected=true` before this run performed its required evidence rotation. The live queue was healthy but not empty: three real searches were waiting normally, no schedule was stale, no pending alert or unresolved feedback existed, and no open support incident touched active real demand. Production error, fatal, warning, 4xx, and 5xx queries since the prior checkpoint were empty.
- Decision: keep repeated access gaps in the machine-readable ranked portfolio with their exact unblock action, but exclude coverage-only signals from automatic implementation selection. A healthy or coverage-only first pass now returns nonterminal `exploration_required`; current-run browser, operations, feedback, search-quality, research, and product-quality evidence must still find safe work or prove the terminal blocker.
- Rotation evidence: signed-out production ZIP `69101` returned Indian Meadows and Lake Maloney on 1440x1000 desktop and 320x800 mobile. Keyboard submission, add/remove ranking, screenshots, images, console/page/network capture, and horizontal-overflow checks were clean; both sessions retained the `AUTOMATION` marker and no customer demand was saved.

## 2026-07-14: Legacy municipal links need identity-aware course handoffs

- Sources: the [City of Winter Park's official WP9 page](https://cityofwinterpark.org/departments/parks-recreation/golf-courses/winter-park-golf-course/), accessed 2026-07-14 America/New_York, identifies Winter Park Golf Course as a public nine-hole course, says public tee times can be made three days ahead, and links `https://winter-park-country-club.book.teeitup.com/` as its official booking surface.
- Tee Time Spot evidence: the saved course still held the former `/golf-course/` URL, which redirects to a municipal index containing both Winter Park Golf Course (WP9) and Winter Park Pines Golf Club (WP18). Monitoring followed neither course detail because the index links contain course identity rather than booking keywords, leaving WP9 `UNKNOWN` through 11 incident observations even though the reusable TeeItUp adapter already exists.
- Decision: after policy links, follow an exact same-origin course-detail label or final path segment before generic booking links. Strip only a trailing parenthetical such as `(WP9)`, require the remaining normalized identity to equal the saved course name, and share fetched pages across courses without sharing identity-specific decisions. This reaches WP9's official TeeItUp link without crossing into the similarly named WP18 sibling.
- Rotation evidence: signed-out production ZIP `83854` returned 15 provider-backed Idaho/Washington-border courses on 1440x1000 desktop and 320x800 mobile. Both `AUTOMATION` sessions showed six initial cards plus a clear expansion control, zero horizontal overflow, no console/page/same-origin request failures, and clean screenshots; no TeeSearch demand was saved.

## 2026-07-14: Embedded municipal content should expose reusable booking links

- Sources: the [Town of Stratford's official Short Beach Golf Course page](https://www.stratfordct.gov/o/stratford/page/short-beach-golf-course), accessed 2026-07-14 America/New_York, says the course is open for the 2026 season, its ForeUp tee sheet is active, and tee times may be booked six days ahead. The page links its public online action to `https://foreupsoftware.com/index.php/booking/19333/145#teetimes`.
- Tee Time Spot evidence: the two affected active searches repeatedly classified Short Beach as `UNKNOWN` / `NEEDS_ADAPTER` even though the official booking link is present in Apptegy's server-rendered page state. The monitoring collector recognized ordinary anchors and embedded navigation `url` fields, but not Apptegy content-button `link` fields; its generic escaped-URL scan also emitted malformed same-origin pseudo-URLs.
- Decision: normalize bounded escaped page-state markup before extracting absolute URLs, recognize paired embedded `title` plus `link` fields alongside existing `title` plus `url` navigation, and feed the resulting official ForeUp URL through the existing reusable adapter. Preserve ordinary HTML anchors, policy-page priority, private/account-required classifications, and all existing provider controls.

- Rotation evidence: signed-out production ZIP `92328` returned Furnace Creek Ranch Golf Course at 6.4 miles on 1440px desktop and 320px mobile. Both marked `AUTOMATION` sessions rendered the same public course without horizontal overflow, console errors, page errors, or failed same-origin requests; screenshots were inspected and no TeeSearch demand was saved.

## 2026-07-14: Account-gated Whoosh availability needs an official-site handoff

- Sources: Yale Golf Course's [official 2026 FAQ](https://yalebulldogs.com/sports/2026/2/27/faqs.aspx), published 2026-02-27 and accessed 2026-07-14, says the course is public but requires Whoosh registration to be confirmed before tee-time availability can be viewed; its [official terms](https://yalebulldogs.com/sports/2026/2/27/terms-and-conditions.aspx), last updated 2026-02, require all tee times to be made online and a credit card to be held for booking.
- Tee Time Spot evidence: the affected active search stored Yale as `UNKNOWN` / `NEEDS_ADAPTER` because official-site discovery followed the Whoosh client shell before the same official site's FAQ. The unauthenticated Whoosh URLs returned only the application shell, while the official FAQ conclusively put availability behind confirmed golfer registration. The alert-only POC does not use course accounts, payment details, or account-specific sessions.
- Decision: inspect same-origin FAQ, terms, and registration guidance before following a booking shell; classify Whoosh as account-required only when the official evidence explicitly says registration confirmation gates availability. Preserve courses whose public availability is visible before registration, keep the official booking link, and resolve the support incident as a direct-booking classification.
- 2026-07-15 correction: the live signed-out Whoosh page contradicted the auxiliary FAQ by exposing Yale tee-time availability publicly. Auxiliary guidance is no longer sufficient for `ACCOUNT_REQUIRED`; the booking surface itself must prove the gate. Yale is now `NEEDS_REVIEW` / `UNSUPPORTED_PLATFORM`, which preserves direct online booking without claiming automatic monitoring.

- Rotation evidence: signed-out production ZIP `83854` returned 15 provider-backed courses on 1440px desktop and 327px mobile. The visible first six remained stable through add/remove, screenshots showed no clipping or horizontal overflow, browser checks had no console, page, or same-origin request failures, the session marker remained `AUTOMATION`, and no TeeSearch demand was saved. `Birdies at the High` shares the Highlands address but lacks current official identity evidence, so it remains an explicit structured-review follow-up rather than motivating a broad proximity or name rule.

## 2026-07-14: Cache successful typed-location geocodes at the edge

- Sources: [Vercel's Cache-Control header reference](https://vercel.com/docs/caching/cache-control-headers), last updated 2026-07-01, documents the default uncached Function response, recommends CDN caching for responses that are the same for every visitor, gives `Vercel-CDN-Cache-Control` top priority for Vercel-only caching, and consumes that header before forwarding the response to browsers. The current [Next.js caching guide](https://nextjs.org/docs/app/getting-started/caching), reviewed against version 16.2.10 on 2026-07-14, keeps Route Handler responses uncached unless the application opts into a cache policy.
- Tee Time Spot evidence: three consecutive production requests for ZIP `49908` all returned `x-vercel-cache: MISS`, `public, max-age=0, must-revalidate`, and the same provider-backed coordinates in 174 to 295 milliseconds. The signed-out follow-through returned no courses at 15 miles and exactly Wyandotte Golf Course and Resort plus Portage Lake Golf Course at 30 miles on 1440px desktop and 320px mobile, with `AUTOMATION` session traffic, zero horizontal overflow, and no console, page, or same-origin request failures.
- Decision: cache only successful typed-location geocode responses at Vercel's edge for 24 hours, allow asynchronous stale revalidation for seven days, and permit one day of stale-if-error resilience while keeping browsers at `max-age=0`. Leave invalid-location and provider-error responses uncached. Do not cache course discovery because its monitoring-support, hole-layout, and recent-price enrichment changes independently of the stable coordinates.

## 2026-07-14: Photo-less recovery results should look intentional

- Sources: the [W3C WAI Images Tutorial](https://www.w3.org/WAI/tutorials/images/), updated 2026-04-08, says image alternatives depend on purpose and decorative media should use a null alternative; the current [Next.js Image reference](https://nextjs.org/docs/app/api-reference/components/image) likewise requires `alt` text that can replace an informative image without changing the page meaning.
- Tee Time Spot evidence: signed-out ZIP `63501` returned one valid public course on 1440px desktop and 320px mobile, then keyboard Enter lookup returned Bethpage Black. Production screenshots showed photo-less lookup data beside an unexplained pale strip on desktop and a 128px empty media block on mobile, even though the adjacent heading and address already identify the course. Both viewports had zero horizontal overflow, console errors, or page errors and used the `AUTOMATION` session marker.
- Decision: keep the missing photo decorative for assistive technology because the adjacent course text already supplies the identity, but add visible icon-and-text placeholder treatment so sighted golfers can distinguish unavailable media from a broken card. Preserve the compact recovery layout and cover the photo-less result in the four-viewport smoke.

## 2026-07-13: Course discovery needs privacy-safe outcome evidence

- Sources: [Vercel's custom-event guidance](https://vercel.com/docs/analytics/custom-events), last updated 2025-09-24, recommends tracking meaningful interactions such as form submissions and organizing results by bounded custom properties; [Vercel's analytics privacy guidance](https://vercel.com/docs/analytics/privacy-policy), last updated 2025-03-04, warns against personal information in custom events; and the [W3C Privacy Principles](https://www.w3.org/TR/privacy-principles/), published 2025-05-15, require transferring only the data necessary for the user's goal.
- Tee Time Spot evidence: the 24-hour public event stream showed page views, one start-search click, and final alert creation, but it could not distinguish course discovery with results, an empty search, or a provider/geocoding failure. The required `58401` rotation returned one course at 30 miles, yet the only analytics event emitted by that browser navigation was a page view. Raw location text, coordinates, Place IDs, and customer identifiers are unnecessary for this funnel question.
- Decision: record only discovery result count, the bounded 5-to-30-mile radius, demo status, or coarse failure stage/status. Summarize 24-hour event counts by `PUBLIC`, `AUTOMATION`, `TEST`, and `UNCLASSIFIED` traffic class so the hourly loop can evaluate real behavior without persistent visitor identifiers or automation contamination.

## 2026-07-13: Restrict unused browser capabilities without breaking location search

- Sources: the [Next.js headers reference](https://nextjs.org/docs/app/api-reference/config/next-config-js/headers), last updated 2026-03-03, documents `Permissions-Policy` as a supported response header configured through `next.config`; [MDN's Permissions-Policy reference](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Permissions-Policy), accessed 2026-07-13 America/New_York, documents allowlists for camera, microphone, payment, USB, browsing topics, and geolocation; [MDN's geolocation directive reference](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Permissions-Policy/geolocation), last modified 2026-01-27, confirms that `geolocation=(self)` preserves top-level and same-origin access while excluding cross-origin frames.
- Tee Time Spot evidence: live `/`, `/search`, and `/api/location/geocode` responses had no `Permissions-Policy`. A repo-wide capability audit found only intentional `navigator.geolocation.getCurrentPosition()` usage and no camera, microphone, Payment Request, WebUSB, or Topics API usage. Signed-out `82901` discovery at 30 miles exercised the real location/search path on 1440px desktop and 320px mobile with the `AUTOMATION` traffic marker, one valid White Mountain Golf Course result, no overflow, and no console, page, or same-origin request failures.
- Decision: preserve geolocation for Tee Time Spot's own origin and disable unused browsing topics, camera, microphone, payment, and USB capabilities across pages and APIs. Leave CSP separate because Clerk authentication, Vercel Analytics, Google-hosted imagery, and inline email previews still need a directive-by-directive compatibility and reporting plan.

## 2026-07-13: Public responses need baseline browser hardening

- Sources: [Next.js security guidance for PWAs](https://nextjs.org/docs/app/guides/progressive-web-apps) recommends `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, and `Referrer-Policy: strict-origin-when-cross-origin`; the [Next.js headers configuration reference](https://nextjs.org/docs/app/api-reference/config/next-config-js/headers), last updated 2026-02-27, documents applying response headers to a wildcard route. [MDN's X-Content-Type-Options reference](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/X-Content-Type-Options), last modified 2026-03-17, explains that `nosniff` makes browsers honor declared MIME types. [OWASP's HTTP Headers Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/HTTP_Headers_Cheat_Sheet.html), accessed 2026-07-13 America/New_York, recommends `nosniff` and `strict-origin-when-cross-origin` on all responses.
- Tee Time Spot evidence: live `https://teetimespot.com/` responses include Vercel HSTS but did not include `X-Content-Type-Options`, `X-Frame-Options`, or `Referrer-Policy`. The site does not need to be embedded by another origin, and golfers regularly leave for official booking pages, so explicit framing and referrer boundaries are applicable without changing product behavior.
- Decision: add the three-header baseline across all routes and verify exact production readback after the Git deployment. Defer CSP and Permissions Policy to a report-only compatibility pass because Clerk, Vercel Analytics, hosted course imagery, and same-origin geolocation need an explicit allowlist audit before enforcement.

## 2026-07-13: Kansas City discovery must exclude a private single-hole pin

- Sources: [Hallbrook Country Club's official site](https://www.hallbrookcc.org/) identifies Hallbrook as a private club; its [official guest rules](https://www.hallbrookcc.org/guest-information) require member sponsorship or limited professional arrangements, and its [tee-time page](https://www.hallbrookcc.org/member-login) requires member login. These current surfaces were accessed 2026-07-13 America/New_York. [Google Places place ID guidance](https://developers.google.com/maps/documentation/places/web-service/place-id), updated 2026-07-08 UTC, confirms that place IDs are reusable stable identifiers but can change and should be refreshed when stale.
- Tee Time Spot evidence: a signed-out `64108` search at the 30-mile ceiling showed 35 results on both 1440px desktop and 320px mobile, labeled the exact Google place `ChIJXdJQJmTpwIcRsoa_jpffsqs` as `18th Hole - Hallbrook CC`, and presented it as `Public`. The result points to Hallbrook's official site but represents one hole inside a private member-controlled course, not a public booking facility. Screenshots showed no horizontal overflow or console/page errors; one desktop Google photo request was browser-blocked while mobile loaded cleanly.
- Decision: exclude only the exact Hallbrook hole-pin place ID before accepting Google type or semantic public-course evidence. Keep ambiguous `Palo parado`, `Golf Course`, and other Kansas City-area pins visible until current official identity/access evidence supports a correction; do not generalize from `CC`, `Club`, or a missing street address.

## 2026-07-13: Phoenix discovery needs official identity recovery

- Sources: [Arizona Grand Resort's official golf page](https://www.arizonagrandresort.com/golf/) identifies one public Arizona Grand Golf Course, links its official tee-time site, and lists the resort at 8000 S Arizona Grand Parkway; [Ahwatukee Golf Club's official site](https://www.ahwatukeegolf.com/) identifies the public course at 12432 S 48th Street with phone 480-893-1161 and its official tee-time link. Both current 2026 surfaces were accessed 2026-07-13 America/New_York. [Google Places place ID guidance](https://developers.google.com/maps/documentation/places/web-service/place-id) says Place IDs can be stored and reused but should be refreshed when stale.
- Tee Time Spot evidence: a real signed-out `85001` search at 30 miles returned 45 records locally and 42 on a production readback. Arizona Grand appeared twice, while the Ahwatukee property appeared as three separate generic `Golf Course` pins. A provider-backed candidate recheck returned a second Arizona Grand secondary identity at 9201 S 51st Street. The official Arizona Grand surface names one course at 8000 S Arizona Grand Parkway, and Ahwatukee's official site names one public course at 12432 S 48th Street.
- Decision: map the exact verified Arizona Grand and Ahwatukee Place IDs to their official identities. When a canonical Place ID is present, suppress its verified aliases; when Google returns only aliases, retain one mapped course so incomplete provider identity data cannot erase a real public course. Leave the unresolved generic `ChIJq6qqBnIGK4cRPpgjylaZoIo` pin in Tempe visible until direct official evidence ties that Place ID to a specific course. Do not infer identity from proximity, a generic name, or city alone.

## 2026-07-13: State-border discovery needs exact identity corrections

- Sources: [The Barn At Fox Run official site](https://www.thebarnatfoxrunvt.com/) (accessed 2026-07-13), [Baker Hill membership FAQ](https://www.bakerhill.org/guest-information/frequently-asked-questions) (accessed 2026-07-13), [Dublin Lake Club private site](https://home.dublinlake.org/default.aspx?E=6&p=home) and [current access listing](https://www.allsquaregolf.com/golf-courses/new-hampshire/dublin-lake-club) (accessed 2026-07-13), [Stratton official course tour](https://www.stratton.com/things-to-do/activities/stratton-golf/tour-the-course) (current 2026 course surface, accessed 2026-07-13), and [Google Places API place IDs](https://developers.google.com/maps/documentation/places/web-service) (updated 2026-07-08, accessed 2026-07-13 America/New_York).
- Tee Time Spot evidence: a real signed-out `05101` search at the 30-mile ceiling returned 11 records spanning Vermont and New Hampshire. The first six visibly labeled the Fox Run wedding venue as `Public` and showed two Google pins for the same Stratton course. Expanding the results exposed Baker Hill, whose official membership process requires an application, interview, sponsors, and recommendation letters, plus Dublin Lake Club's member-gated course. Google continued to return every record as a golf course and the semantic public-course query corroborated them.
- Decision: exclude the exact Barn, Baker Hill, and Dublin Lake place IDs before accepting type or public-query evidence, and map the secondary Stratton pin to its official 251 Stratton Mountain Road identity. Keep Hooper, Bretwood, Fox Run, the canonical Stratton record, and other unverified facilities visible. Stable IDs avoid broad `Barn`, `Club`, or same-website heuristics that could remove real public facilities or distinct resort layouts.
## 2026-07-13: Empty searches need bounded recovery and feedback needs focus management

- Sources: [W3C WAI-ARIA Dialog (Modal) Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/) and [W3C Modal Dialog Example](https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/examples/dialog/) (last updated 2026-03-04), accessed 2026-07-13 America/New_York.
- Tee Time Spot evidence: a real signed-out `59001` search returned zero public courses at the default 15 miles on desktop and mobile. The page stated the count but offered no direct next step even though the product contract already defines a 5-to-30-mile discovery range. The live controls had drifted to 1-to-50 miles. Keyboard testing also found that opening the feedback panel left focus on the document body, exposed no dialog landmark, ignored Escape, and returned focus to the body after closing.
- Decision: restore the documented 5-to-30-mile range on both search entry points, turn zero results into an announced action that can rerun the same coordinates at 30 miles, and keep the course-name lookup as the fallback after the full range is exhausted. Give the non-modal feedback panel a programmatic dialog name, move focus inside when it opens, close it with Escape, and return focus to the launcher when it closes.

## 2026-07-13: Miami discovery needs stable identity exclusions

- Sources: [Green Girls Golf official site](https://www.greengirlsgolf.com/), [Golf Miami 305 official site](https://www.golfmiami305.com/), [Shell Bay Club official site](https://shellbayclub.com/), [South Florida Golf Magazine business listing](https://nextdoor.com/pages/south-florida-golf-magazine-miami-beach-fl/), and [Celebrity Amputee Golf Classic nonprofit record](https://projects.propublica.org/nonprofits/organizations/454693128), accessed 2026-07-13 America/New_York.
- Tee Time Spot evidence: a real signed-out `33139` search at 30 miles returned Green Girls Golf, South Florida Golf Magazine, and Celebrity Amputee Golf Classic among the first six results, ahead of actual public courses. The full API payload also returned the Golf Miami 305 simulator, a PGA TOUR delivery listing, and invitation-only Shell Bay Club. All were labeled `Public` because Google still supplied them as `golf_course` records and the semantic public-course query corroborated their place IDs.
- Decision: exclude the six exact Google place identities before type or semantic-public corroboration. Green Girls Golf is a caddie/event service, South Florida Golf Magazine is a publication, Celebrity Amputee Golf Classic is a nonprofit event, Golf Miami 305 is an indoor Trackman facility, PGA TOUR Deliveries is a delivery listing, and Shell Bay documents private membership. Do not add broad `Classic`, `Magazine`, `Deliveries`, or generic business-name heuristics; the ambiguous `xie` and generic `Golf Course` pins remain visible until their identities are resolved.

## 2026-07-13: Search needs programmatic link names, AA contrast, and a discoverable LCP image

- Sources: [WCAG 2.2 SC 1.4.3 Contrast Minimum](https://www.w3.org/TR/WCAG22/#contrast-minimum), [W3C SC 2.4.4 Link Purpose](https://www.w3.org/WAI/WCAG22/Understanding/link-purpose-in-context.html) (updated June 2026), [Next.js Image documentation](https://nextjs.org/docs/pages/api-reference/components/image) (updated April 15, 2025), and [web.dev LCP guidance](https://web.dev/articles/optimize-lcp), accessed 2026-07-13 America/New_York.
- Tee Time Spot evidence: a production mobile Lighthouse run on `/search` scored 72 performance and 92 accessibility, with a 7.5-second lab LCP, a 428 KiB 2400px CSS background image that was not discoverable in the initial document or high priority, two unnamed compact nav links, and 13 low-contrast text failures between 2.29:1 and 4.1:1. Browser rotation at 320px with forced colors and reduced motion otherwise showed no overflow, small targets, keyboard failure, console error, or failed request.
- Decision: render the decorative hero through Next Image with responsive `sizes`, eager loading, and high fetch priority; preserve the overlay and crop in CSS. Give compact nav links stable accessible names, darken the affected label/support colors above the 4.5:1 AA threshold, and keep the mobile feedback launcher in document flow so it cannot cover the primary Search button. Desktop feedback remains fixed.

## 2026-07-13: Verified simulator identity must override a stale golf-course type

- Sources: [Chicago Golf Authority: How It Works](https://chicagogolfauthority.com/pages/how-it-works) and [Google Places API (New) place types](https://developers.google.com/maps/documentation/places/web-service/place-types), accessed 2026-07-13 America/New_York. The official business page identifies the West Loop address as an indoor simulator facility; Google's current type catalog distinguishes `indoor_golf_course` from `golf_course`.
- Tee Time Spot evidence: a real signed-out `60601` search returned Google place `ChIJy4_CTDEtDogR9wxAr-a-VGI` first and labeled it `Public`, even though it is a suite-based simulator rather than a playable outdoor course. Production still supplies the place through the golf-course query, so type filtering alone does not catch it.
- Decision: exclude this verified non-course by stable Google place ID before accepting type or semantic-public corroboration. Do not add a broad `Authority` or suite-address heuristic; ambiguous generic Chicago course pins remain visible until their identities can be resolved safely.

## 2026-07-13: Sitemaps should cover canonical public routes with trustworthy fields

- Sources: [Google Search Central: Build and submit a sitemap](https://developers.google.com/search/docs/crawling-indexing/sitemaps/build-sitemap) (last updated 2025-12-10 UTC) and [Next.js sitemap file convention](https://nextjs.org/docs/app/api-reference/file-conventions/metadata/sitemap) (last updated 2026-03-25), accessed 2026-07-13 America/New_York.
- Finding: sitemaps should list the canonical absolute URLs intended for search results. Google only uses `lastmod` when it consistently reflects a significant page update, and ignores `changefreq` and `priority`; Next.js supports a typed `sitemap.ts` route for programmatic URL generation.
- Tee Time Spot evidence: production `/search` is indexable, has its own canonical URL, and is the primary product workflow, but the live sitemap listed only `/`. The sitemap also generated deployment-time freshness plus `changefreq` and `priority` values that were not backed by page-specific update evidence.
- Decision: list both canonical public routes (`/` and `/search`) and omit guessed or ignored freshness/priority fields. Keep account, email-preview, and signed action routes out because their metadata is explicitly `noindex`.

## 2026-07-13: Invalid locations need descriptive, announced recovery copy

- Sources: [W3C Understanding SC 3.3.1: Error Identification](https://www.w3.org/WAI/WCAG22/Understanding/error-identification) (updated 2026-03-09) and [W3C Understanding SC 4.1.3: Status Messages](https://www.w3.org/WAI/WCAG22/Understanding/status-messages), accessed 2026-07-13 America/New_York.
- Finding: automatically detected input errors should identify the affected input, describe the problem in text, and expose dynamically inserted error messages through an appropriate alert or live-region role.
- Tee Time Spot evidence: a signed-out invalid-location search rendered the raw JSON payload `{"error":"No matching location found."}` on desktop and mobile, and the API classified the correctable input miss as a 502.
- Decision: parse structured API errors into plain recovery copy, mark and associate the location input with the alert, and return 404 for an unmatched location while preserving 502 for genuine provider failures.

## 2026-07-12: Official access evidence must override Places public-query recall

- Source: [Shelter Harbor Golf Club official site](https://www.shgcri.com/) and its [official membership page](https://www.shgcri.com/default.aspx?p=dynamicmodule&pageid=407717&ssid=334819&vnf=1), accessed 2026-07-12 America/New_York. The home page identifies the facility as a member-owned private equity club, and the membership page says membership is by invitation.
- Observed gap: a real signed-out `02891` search returned Shelter Harbor from both Google's golf-course data and the semantic `public golf courses` query, so Tee Time Spot labeled it `Public` despite the official access policy.
- Product decision: stable place-ID exclusions backed by current official access evidence override Google type and text-search corroboration. Name-only `Golf Club` exclusions remain inappropriate because many public facilities use that wording.

## 2026-07-13: Public Chronogolf profiles can restore alert monitoring

- Sources: [Blue Rock Golf Course's official site](https://bluerockgolfcourse.com/) says the course is open to the public and links its [official Chronogolf profile](https://www.chronogolf.com/club/blue-rock-golf-course). The profile's current public page data identifies club `7221`, course `7657db51-4e0c-4bc7-8e98-bd0a705370af`, and online booking as enabled. Chronogolf's read-only public marketplace endpoint returned current tee-time records without an account, checkout, or verification flow. These surfaces were accessed 2026-07-13 America/New_York.
- Tee Time Spot evidence: the browser-discovery worker recognized the Chronogolf platform but only learned numeric `/club/<id>` URLs. Blue Rock therefore remained `UNKNOWN` with an open support incident even though its current official profile uses a descriptive slug and exposes stable club/course identifiers.
- Decision: enrich both numeric and slug-form official Chronogolf profiles, store only reusable club/course metadata, and query the public marketplace endpoint for alert matching. Keep the adapter read-only and link golfers back to the official profile to book. Dennis Highland and Dennis Pines remain separate `NEEDS_ADAPTER` work because their Chelsea Reservations surface is account-oriented and was not traversed.

## 2026-07-13: Chelsea exposes a public non-member tee sheet before reservation

- Sources: [Dennis Golf's official site](https://www.dennisgolf.com/) links directly to the shared [Dennis Chelsea Reservations surface](https://dennis.chelseareservations.com/), whose `Non Members Login` route displays course, date, time, player-count, holes, and open-slot controls while explicitly saying `You are Not Logged in`. The Town of Dennis [current policies and procedures](https://www.dennisgolf.com/wp-content/uploads/2025/08/Policies-and-Procedures-Amended-SB-08.26.2025-Current.pdf) state that non-members may request tee times seven days in advance by phone or through the online reservation system without prepayment. These sources were accessed 2026-07-13 America/New_York.
- Tee Time Spot evidence: the active four-player Cape Cod search had unresolved incidents for both Dennis Highland and Dennis Pines. A read-only four-player probe of the public page returned current Highland availability without a member account, payment, captcha, verification flow, or selection of the provider's `Reserve` action.
- Decision: treat Chelsea as a reusable read-only `CUSTOM` provider. Persist the public tenant URL plus the provider's separate Highland/Pines course codes, carry the official seven-day non-member booking window into scheduling, parse only displayed availability, and leave every reservation step on the official site.

## 2026-07-13: Current CPS public engines can expose availability without a token

- Sources: [Yarmouth Golf's official booking page](https://www.golfyarmouth.com/contact-us-/book-now) separates `Public Tee Times` from the member portal and links the public action to [Yarmouth Public CPS](https://yarmouthpublic.cps.golf/). CPS's [current public booking help](https://onlinehelp.cps.golf/help/Content/ORE05%20and%20OO/ORE05/Customer-Guide/2Where%20to%20start%20at%20the%20Online.htm) documents that configured public reservation sites may open directly on searchable tee times. These sources and the public site's own configuration/network surface were inspected 2026-07-13 America/New_York.
- Tee Time Spot evidence: Bayberry Hills was stored against the unrelated `sc.cps.golf` webstore and failed at configuration with 404. The current `yarmouthpublic` engine publishes a runtime API key in its public configuration, resolves the public website ID through `GetAllOptions`, and loads `OnlineCourses` and `TeeTimes` without an authorization token, account, checkout, or transaction-registration request.
- Decision: keep the existing short-lived-token CPS flow for legacy tenants, add the provider's current public API-key mode as a read-only alternative, and persist Bayberry's official public tenant plus exact Red/Links course IDs. Never persist or log the provider-published runtime key; load it from the public configuration on each check.

## 2026-07-14: Provider golf-course types still need exact access evidence

- Sources: [Google Places API (New) place types](https://developers.google.com/maps/documentation/places/web-service/place-types), [NEXUS Golf Club](https://www.nexusgolf.com/contact), [Liberty National guest information](https://www.libertynationalgc.com/public-golf), [Bayonne Golf Club membership](https://www.bayonnegolfclub.com/Membership), [Forest Hill Field Club](https://foresthillfc.com/about), and [Montclair Golf Club](https://www.montclairgolfclub.org/), accessed 2026-07-14 America/New_York.
- Tee Time Spot evidence: a real signed-out `10001` search returned 31 records. NEXUS, Liberty National, BackyardSwingsStudio, and Bayonne occupied four of the first six positions; Forest Hill, Montclair, and the apartment-address `Q5C9+8VQ New York` record also appeared. Current Google detail responses identify all seven as operational primary `golf_course` records even though the official sites identify NEXUS as an indoor membership facility and the four named clubs as private or member-and-guest access.
- Decision: exclude the seven exact stable Google identities before type or semantic-public corroboration. Preserve Skyway, Forest Park, and the similarly named verified-public Chris Bargas Golf Club as positive controls. Do not infer access from `Golf Club`, `Field Club`, an apartment address, or membership-related Places types alone. Because exact registries now span repeated failure families, the next systemic learning target is a reviewable structured access-evidence pipeline that captures official public/private/indoor proof without turning one market into a broad name heuristic.

## 2026-07-14: Search progress must replace stale result announcements

- Source: [W3C ARIA22: Using `role=status` to present status messages](https://www.w3.org/WAI/WCAG21/Techniques/aria/ARIA22), accessed 2026-07-14 America/New_York. The current technique uses search-result updates as its primary example, notes that `status` is a polite live region, and recommends explicit `aria-atomic="true"` when the entire message should be announced consistently.
- Tee Time Spot evidence: a signed-out `70358` search on 1440px desktop and 320px mobile correctly broadened from 15 to 30 miles, but the existing 15-mile empty state was immediately relabeled as the final 30-mile no-result message while the wider provider request was still pending. That could announce a false zero-result outcome before a late result arrived.
- Decision: replace stale result or empty-state copy with one atomic `Searching public courses within …` status for every discovery request, then render and announce the final result only after the response completes. Keep the current search controls and bounded 30-mile recovery behavior.

## 2026-07-14: Maintenance facilities need a multi-market course filter

- Sources: [Google Places API Place resource](https://developers.google.com/maps/documentation/places/web-service/reference/rest/v1/places) (updated June 2026), [Tahoe Paradise Golf Course directions](https://www.tahoeparadisegc.com/directions), [Carson Valley Golf Course contact](https://carsonvalleygolf.com/contact/), and [Great Parks' official Mill Course page](https://www.greatparks.org/parks/winton-woods/the-mill-course), accessed 2026-07-14 America/New_York.
- Tee Time Spot evidence: signed-out provider-backed discovery returned `TPGC Maintenance Area` and `Carson Valley Golf Course Maintenance` around `96120`, plus `Winton Woods Operations` around `41011`, all as operational primary `golf_course` records. The same snapshots separately returned the playable Tahoe Paradise, Lake Tahoe, Carson Valley, Winton Woods, and Mill courses. Current Places detail reads still label each facility as a golf course, so provider type, semantic public-query corroboration, and the new `googleMapsTypeLabel` field do not distinguish them. The official course sites identify different playable-course names and addresses.
- Decision: filter explicit `maintenance`, `maintenance area/facility/shop`, and `operations` facility names using a two-market corpus, not stable-ID registry growth. Preserve seven similarly named public-course, resort, multi-use, and facility controls in focused coverage. Leave `Madden Memorial Fund` visible for now: it is a separate nonprofit-identity failure that needs structured official-access evidence rather than a one-example `Fund` name rule.

## 2026-07-14: Homepage supporting text must meet minimum contrast

- Source: [WCAG 2.2 Success Criterion 1.4.3: Contrast (Minimum)](https://www.w3.org/TR/WCAG22/#contrast-minimum), accessed 2026-07-14 America/New_York. Normal text requires at least 4.5:1 contrast; the 3:1 exception applies only to large text.
- Tee Time Spot evidence: a fresh production mobile Lighthouse run scored 96 for accessibility and identified homepage labels and supporting copy between 2.30:1 and 4.46:1. The affected text included scenario labels, form labels, distance endpoints, empty selected-course guidance, and Discord community copy. The same run scored 81 for performance with a 4.5-second text LCP, while image delivery reported no savings, so the measured accessibility defect outranked a speculative image change.
- Decision: raise only the affected foreground colors while preserving the current layout, backgrounds, hierarchy, and alert-only copy. Add four-viewport Playwright assertions for the corrected color tokens and require a post-change Lighthouse accessibility score of 100.

## Product Shape

Tee Time Spot should behave like a waitlist assistant rather than a booking marketplace. GolfNow/TeeOff-style products emphasize inventory search, deals, and broad marketplace browsing. Noteefy-style flows emphasize preference capture, waitlist matching, and alerts.

## UX Principles For Automation Cycles

- First screen: make Tee Time Spot, nearby discovery, and alert-only booking clear immediately.
- Course selection: keep the 1 to 5 ranking interaction visible and low-friction.
- Course list: use Google Places photos as a scanning aid when available, keep stable placeholders when not, and show required photo attribution.
- Dashboard: show active searches, ranked courses, probe state, pending matches, and pause/cancel controls.
- Trust: state that users finish booking on the official course site.
- Improvement loop: every automation cycle should inspect onboarding, course ranking, dashboard state, and email copy for friction.
- If the design does not look good in browser screenshots, do a tool research pass and try a better workflow instead of polishing a weak direction.

## Reference Patterns

- Nearby course search: location permission plus typed city/ZIP fallback.
- Waitlist alerts: one user intent, durable preferences, notification when inventory changes.
- Course policy handling: if terms prohibit automated access, skip and record the blocker.

## Current Tool Candidates

Use these as starting points, then refresh before adopting because design tooling changes quickly.

- Figma AI and Figma Make: useful for alternate visual directions, prototypes, annotations, and design iteration.
- v0 by Vercel: useful when the desired output is React/Next.js UI code or a live prototype that can be brought back into the repo.
- Lovable/Replit/Bolt-style app builders: useful for comparing full-app product flows, but generated code must still be reviewed, tested, and adapted to this repo.

## 2026-07-12 Mobile Search Reflow

- Source: [W3C Understanding SC 1.4.10: Reflow](https://www.w3.org/WAI/WCAG21/Understanding/reflow) (accessed 2026-07-12; no page update date shown).
- Finding: ordinary controls and text should remain readable at a 320 CSS-pixel-equivalent width, and stacking sections into one column is a common responsive approach when the layout does not require two dimensions.
- Tee Time Spot evidence: a production check at 375 CSS pixels showed the two-column search grid clipping the location and course-local time values even though the document itself did not overflow horizontally.
- Decision: stack the four primary search fields into full-width mobile rows, keep the desktop grid unchanged, and cover field width plus singular result-count copy in Playwright.

## 2026-07-13: Keep auth UI ready and disable optional SDK telemetry

- Sources: [Clerk `<ClerkProvider>` reference](https://clerk.com/docs/reference/components/clerk-provider) (published 2026-07-08; accessed 2026-07-13 America/New_York) and [Clerk Next.js rendering modes](https://clerk.com/docs/guides/development/rendering-modes) (published 2026-07-08; accessed 2026-07-13 America/New_York).
- Finding: Clerk supports disabling its optional SDK telemetry independently from the provider, account state, and prebuilt authentication UI. Although `prefetchUI={false}` can skip the UI preload for headless/custom auth surfaces, it is not compatible with Tee Time Spot's current prebuilt modal flow.
- Tee Time Spot evidence: a fresh mobile Lighthouse run against production `/search` scored 83 for performance with 4.1-second LCP and an estimated 361 KiB of unused JavaScript. Clerk UI and vendor bundles accounted for about 183 KiB of that estimated waste before a golfer opened any account surface; accessibility, best practices, and SEO each scored 100.
- Decision: retain UI prefetch so the sign-in modal remains reliable, keep the root provider for signed-in state, `UserButton`, dashboard ownership, and alert creation, and disable only Clerk's optional telemetry because Tee Time Spot does not use that signal. A production smoke against the first attempt proved the modal failed in all four viewports, so UI deferral was rejected and repaired before closeout. Treat the decorative search-header image identified as the 4.1-second LCP as a separately planned performance candidate that requires a pre-edit path claim.

## 2026-07-13: Keep client-only search prefill out of request-time rendering

- Sources: [Next.js layouts and pages](https://nextjs.org/docs/app/getting-started/layouts-and-pages) (current documentation; no update date shown in the retrieved page) says reading the page `searchParams` prop opts a route into dynamic rendering, while client-only URL state belongs in a Client Component. [web.dev TTFB guidance](https://web.dev/articles/optimize-ttfb) (updated 2025-11-28) identifies TTFB as a prerequisite for every later paint metric and notes that CDN-cached static responses avoid request-time rendering work. Sources accessed 2026-07-13 America/New_York.
- Tee Time Spot evidence: the production `/search` route returned `x-vercel-cache: MISS` plus `private, no-cache, no-store` on three consecutive requests because the Server Component read URL prefill values. A fresh simulated mobile Lighthouse run scored 91 performance with 3.4-second LCP; all prefill values only initialize client form controls and do not load server data. Signed-out `96120` desktop and 327px mobile exploration otherwise showed no overflow, console errors, or failed requests.
- Decision: prerender `/search` and read validated direct-link values in the existing client-side prefill transfer. Session-storage navigation remains single-use and takes precedence, direct URLs retain location/date/time/player/layout/radius/coordinate behavior, malformed values remain bounded, and no customer demand or persistent visitor identifier is created.

## 2026-07-14: Make private-course access exclusions reviewable

- Sources: [Baywood Golf & Country Club's official membership page](https://baywoodgcc.com/membership/) and [official home page](https://www.baywoodgcc.com/), accessed 2026-07-14 America/New_York. The membership surface requires golf membership, board approval, monthly dues, and member-controlled guest privileges; the home page exposes tee-time booking specifically for members.
- Tee Time Spot evidence: a signed-out production search for `95501` returned three results on 1440px desktop and 320px mobile and labeled exact Google place `ChIJtVWUTNtX0VQR4I_SquYKOr4` (Baywood Golf & Country Club) as `Public`. Eureka Municipal Golf Course and Beau Pre Golf Course were the two valid same-market controls. Keyboard add/remove, screenshots, console/network inspection, analytics traffic-class readback, and overflow checks were otherwise clean, and no demand was persisted.
- Decision: exclude only Baywood's exact stable place ID and back the private-course registry with explicit `PRIVATE_MEMBER_CONTROLLED`, official evidence URL, and review-date fields. Do not infer private access from `Country Club`, membership-related Places types, or proximity. Preserve Eureka Municipal, Beau Pre, the similarly named verified-public Grassy Hill Country Club, and all existing public/resort controls.
- Candidate comparison: current [Resend deliverability guidance](https://resend.com/docs/dashboard/emails/deliverability-insights) recommends a plain-text alternative, but Resend's [August 21, 2025 automatic plain-text release](https://resend.com/changelog/automatic-plain-text-emails) confirms the current Node SDK already generates one from Tee Time Spot's HTML, so no duplicate email renderer was added. Sparse unclassified analytics traffic also lacked evidence of a repo-owned regression. The verified live private-course result therefore ranked first.

## 2026-07-14: Classify verified non-course identities for review

- Sources: [ParTee Golf & Games' official contact page](https://parteegolfgame.com/contact), accessed 2026-07-14 America/New_York, identifies the exact 714 Main Street location as an indoor venue with five golf simulator bays, virtual reality, arcade games, and other entertainment rather than a playable outdoor course. [Google's current Core Web Vitals guidance](https://web.dev/articles/vitals), last updated 2024-10-31 and accessed 2026-07-14, recommends field measurement of LCP, INP, and CLS at the 75th percentile.
- Tee Time Spot evidence: a signed-out production search for state-border ZIP `99179` returned three records and labeled Google place `ChIJu4ODUC01oFQRrHyJkM_URz4` (`PARTEE GOLF AND GAMES, LLC`) as `Public` even though its official surface is simulator-only. Quail Ridge Golf Course and Bryden Canyon Public Golf Course were the two valid same-market controls. The baseline build and 60-test desktop/mobile smoke passed, all browser analytics carried `AUTOMATION`, and no customer demand was saved.
- Decision: exclude only ParTee's exact stable place ID before accepting Google's stale `golf_course` type or semantic public-course corroboration. Do not add a broad `ParTee`, `Games`, entertainment, address, or proximity heuristic. Preserve Quail Ridge, Bryden Canyon, a similarly named legitimate `Par Tee Golf Course`, a public resort control, the verified-public Grassy Hill override, and the existing full corpus.
- Systemic learning step: the non-course registry already spans more than three exact corrections, so every entry now records a bounded classification, official evidence URL, and review date. This makes simulator, business, venue, service/media, event, and delivery corrections auditable without weakening provider-field validation or growing an opaque ID set.
- Candidate comparison: Vercel Speed Insights is already mounted through the site's traffic-sanitizing observability component, while a fresh PageSpeed field-data request was quota-limited and the current public funnel contained two successful searches with no public discovery failures. Additional performance instrumentation or analytics changes therefore lacked a measured repo-owned gap; the reproduced false course ranked first.

## 2026-07-14: Restore native keyboard submission for course discovery

- Sources: [WHATWG HTML implicit form submission](https://html.spec.whatwg.org/multipage/form-control-infrastructure.html#implicit-submission) (living standard updated 2026-07-08 in the retrieved result) and [W3C WCAG 2.2 SC 2.1.1 Keyboard](https://www.w3.org/TR/WCAG22/#keyboard) (W3C Recommendation; accessed 2026-07-14 America/New_York).
- Finding: a form with an enabled default submit button supports the platform's Enter-key implicit submission behavior, while WCAG requires functionality to remain operable through a keyboard interface.
- Tee Time Spot evidence: in a fresh marked production desktop session, entering rural ZIP `84728` and pressing Enter in the location field made no geocode or discovery request and left the initial guidance unchanged. Clicking Search immediately ran the same input through clean 15- and 30-mile provider responses. Mobile behavior was inconsistent across runs, while screenshots, overflow, console, page-error, and same-origin request checks were otherwise clean.
- Decision: make the existing course-filter toolbar a semantic form, route its submit event through the existing bounded discovery handler, and make Search its default submit button. Keep location, radius, layout, no-result recovery, analytics traffic class, and alert-saving behavior unchanged. The main desktop/mobile Playwright journey now starts discovery by pressing Enter, while other scenarios continue to cover pointer activation.
- Candidate comparison: the signed-out `84728` search result itself was rejected as a discovery defect because both provider calls returned 200 with no courses and no reported missing course. The current queue also had zero pending alerts, actionable probes, open support incidents, or unresolved feedback, so the reproducible keyboard gap ranked above speculative search or delivery work.

## 2026-07-14: Measure the selection-to-sign-in funnel without identifying golfers

- Sources: [Vercel custom-event guidance](https://vercel.com/docs/analytics/custom-events) (updated 2025-09-24) recommends recording deliberate user actions such as button clicks in the action handler. [Vercel privacy and compliance guidance](https://vercel.com/docs/analytics/privacy-policy) (updated 2025-03-04) recommends aggregate measurement without personal identifiers and explicitly warns against sending email addresses or other sensitive values in custom-event data. Sources accessed 2026-07-14 America/New_York.
- Tee Time Spot evidence: the live 24-hour first-party funnel showed 54 `PUBLIC` page views, four completed discoveries, four search-submission events, three start-search clicks, and no public discovery failure. It did not record whether a signed-out golfer selected a course or clicked the alert CTA before authentication, so discovery abandonment and sign-in friction were indistinguishable. A marked rural `98237` production check correctly broadened from zero results at 15 miles to Skagit Preserve and Overlook at 30 miles; desktop and 327px mobile screenshots, sign-in modal checks, overflow checks, and browser failure capture were clean, and no demand was persisted.
- Decision: add one `course_selection_started` milestone per mounted search experience and an `alert_sign_in_clicked` milestone at the signed-out CTA. Store only selected-course count, player count, optional physical-layout preference, page path, discovery-source bucket, and aggregate traffic class. Do not store course identity, location, email, account ID, referrer detail, or any visitor/session identifier. Keep `search_submitted` as the server-accepted completion milestone.
- Candidate comparison: the `98237` provider results were internally consistent and preserved both plausible public-course controls, so no search/discovery change was justified. The instrumentation gap was a reproducible analytics/observability defect and won the diversity comparison after accessibility/UI and reliability/security releases.

## 2026-07-14: Treat an exact Places type as evidence, not proof of course identity

- Sources: [Google Places API Place Types (New)](https://developers.google.com/maps/documentation/places/web-service/place-types) and [Place Details (New)](https://developers.google.com/maps/documentation/places/web-service/place-details), both current and accessed 2026-07-14 America/New_York, define `primaryType`, `types`, stable place IDs, and field-mask behavior. The [City of Orlando's Dubsdread location page](https://www.orlando.gov/Our-Government/Departments-Offices/Executive-Offices/CAO/Sustainability-Resilience/Green-Works-Focus-Areas/Alternative-Transportation/Expanding-Public-Electric-Vehicle-EV-Charging-Infrastructure/Dubsdread-Golf-Course-Station), accessed 2026-07-14, identifies the actual municipal course at 549 West Par Street; Dubsdread's current official site independently identifies the same public course, address, phone, and direct tee-time surface.
- Tee Time Spot evidence: current Place Details returned exact ID `ChIJa52pBnx754gRry26du_jGzo` as operational with `primaryType=golf_course`, but the record named `BagBoyz2` at 401 Golfview Street had no rating, phone, website, or independent course surface. A live 15-mile Orlando response placed it first at zero distance alongside the real Dubsdread result. Exact-name research returned a music title rather than a playable course, while the City and course operator identify Dubsdread as the real nearby public facility.
- Decision: add one reviewed `VERIFIED_NON_COURSE` correction for the exact stable place ID. Do not add a `Bag`, `Boyz`, missing-website, address, distance, or type-based heuristic. The live nearby result changed from 19 to 18 with only BagBoyz2 removed; Dubsdread and Winter Park remained exactly once. Focused controls also retain a public resort, a similarly named legitimate course, and the verified-public Grassy Hill override.
- Candidate comparison: the prior-window log scan contained no warning, error, fatal, or 5xx rows. Bot 404s and deployment-verification 416 range probes were self-induced or non-customer traffic, while the BagBoyz2 support incident affected an active search and had repeated 16 times. The incident therefore outranked a discretionary non-search tooling follow-up under the active-incident exception.

## 2026-07-14: Carry synthetic provenance into saved demand

- Sources: [OpenTelemetry user-agent semantic attributes](https://opentelemetry.io/docs/specs/semconv/registry/attributes/user-agent/) (current registry; accessed 2026-07-14 America/New_York) defines a bounded synthetic type so test/bot traffic can be distinguished from genuine client traffic. [Playwright browser-context isolation](https://playwright.dev/docs/browser-contexts) (current documentation; accessed 2026-07-14 America/New_York) confirms that each test context has isolated session storage, matching Tee Time Spot's per-tab aggregate marker.
- Tee Time Spot evidence: funnel events already separated `TEST` and `AUTOMATION`, but `TeeSearch` did not retain that provenance. A read-only eight-hour correlation found 31 active searches and 31 accepted `search_submitted` events: 30 searches matched a `TEST` event 0.5 to 2.4 seconds later with the same player count, course count, and requested layout; the one genuine search matched a `PUBLIC` event. Synthetic probes and 77 support incidents therefore competed with real demand, including a fresh CPS 403 and generic `Golf Course` candidate that both belonged to marked test searches.
- Decision: persist only the existing four-value aggregate traffic class on each saved search, with no visitor or session identifier. New saves carry the browser marker through a bounded header; unknown values fail closed to `UNCLASSIFIED`. The additive migration backfills only synthetic rows with a following synthetic submission event that agrees on all low-cardinality dimensions. Hourly active-demand counts, pending alerts, probes, and incidents exclude `TEST` and `AUTOMATION`, while inspection continues to show them explicitly.
- Rotation and candidate comparison: signed-out ZIP `98660` returned 11 cross-border Vancouver/Portland locations on desktop and 320px mobile with clean screenshots, no overflow or browser failures, working course ranking, `AUTOMATION` traffic, and no saved demand. `Hole 16` and `Golf Course Hole #5` are a credible search-quality follow-up, but a broad hole-name rule from one market would violate positive-control requirements. The provenance defect ranked first as a measured reliability/observability issue; the hole-result family requires place IDs, exact before/after accounting, official identity evidence, and multi-market public-course controls in a later run.

## 2026-07-14: Prefer Google's explicit private-course label over legacy golf typing

- Sources: [Google Places API (New) release notes](https://developers.google.com/maps/documentation/places/web-service/release-notes), accessed 2026-07-14 America/New_York, document the February 12, 2026 `googleMapsTypeLabel` field; [Place Types (New)](https://developers.google.com/maps/documentation/places/web-service/place-types), current and accessed the same day, explains that a place has one supported primary type but can carry multiple descriptive types. [The Highlands Golf Course](https://thehighlandsgc.com/) and [The Coeur d'Alene Resort Golf Course](https://www.floatinggreen.com/) identify the same-market public and resort controls and distinguish their playable courses from restaurants, amenities, and individual features.
- Tee Time Spot evidence: a signed-out state-border search for ZIP `99019` returned 16 locations on 1440px desktop and 320px mobile. Live Place Details labeled `CDA National Reserve Main Gate`, `The Golf Club At Black Rock`, and `CDA National Lake House` as `Private Golf Course` even though each retained the legacy `primaryType=golf_course` shape that the app accepted. Add/remove ranking, screenshots, 320px reflow, console/network capture, and `AUTOMATION` traffic marking were clean, and no demand was persisted.
- Decision: request the current Maps label in bounded Nearby and Text Search field masks with a stable English language code and reject only the exact normalized `Private Golf Course` label after exact reviewed overrides. A `VERIFIED_PUBLIC` review remains authoritative. Focused controls preserve Liberty Lake, Coeur d'Alene Public, the Coeur d'Alene resort, both Pinehurst No. 2 and No. 4 at one facility, and verified-public Grassy Hill.
- Measured provider-backed result: the local production-mode API changed from 16 to 13 results, removing only the three provider-labeled private records. All 13 other IDs remained in the same distance order. `Birdies at the High`, the Floating Green feature, the Spokane County office, and two generic `Golf Course` records remain separate exact-identity candidates because the current label does not safely distinguish them; they need stable-ID official evidence or a representative systemic corpus before another rule.
- Candidate comparison: the prior-window log scan contained one intentional unauthenticated `/api/searches` 401 and no warning, fatal, or 5xx rows; all 77 open incidents belonged only to `TEST` searches. Lowering expected-auth log severity and bounding synthetic-search lifecycle remain valid observability/reliability follow-ups, but the reproduced public discovery defect had a current structured provider signal and the strongest immediate user impact.

## 2026-07-14: Keep synthetic checks out of the owner incident queue

- Sources: [OpenTelemetry HTTP semantic conventions](https://opentelemetry.io/docs/specs/semconv/http/http-spans/) (current specification; accessed 2026-07-14 America/New_York) defines a bounded `user_agent.synthetic.type` signal for test or bot traffic, and [Playwright browser-context isolation](https://playwright.dev/docs/browser-contexts) (current documentation; accessed the same day) treats each automated context as isolated test state.
- Tee Time Spot evidence: production inspection found 31 active `TEST` searches, three `UNCLASSIFIED` searches, zero non-synthetic actionable probes, zero pending alerts, no unresolved feedback, and 77 open support incidents whose active demand was exclusively `TEST`. Those synthetic searches still exercised real workflows and repeatedly incremented internal incidents even though the hourly owner queue correctly excluded them. The prior-hour production log scan had no warning, error, fatal, 4xx, or 5xx rows.
- Decision: keep synthetic workflow evidence intact but stop `TEST` and `AUTOMATION` searches from opening or inflating owner-facing `CourseSupportIncident` rows. On a later synthetic check, close an existing unnotified incident only when the course has no active non-synthetic demand. If real demand shares the course, leave the incident untouched so adapter remediation remains first-class.
- Rotation and candidate comparison: signed-out ZIP `57501` returned Dunes Golf Complex and Codgers Caddy Shack on 1440px desktop with no overflow, console errors, or failed requests. A 320px invalid-location run showed clear recovery copy and no overflow. Codgers Caddy Shack remains a search-quality identity candidate requiring its stable Place ID and official evidence; the measured 77-row synthetic incident queue ranked first because it was a proven reliability/observability defect and provided the required non-search category rotation after one discretionary search release.

## 2026-07-15: Bound synthetic workflow lifetime and exclude exact parking identity

- Sources: [Google Places API Place Types (New)](https://developers.google.com/maps/documentation/places/web-service/place-types), last updated 2026-07-10 and accessed 2026-07-15 America/New_York, distinguishes `parking` from `golf_course` while explaining that provider places can carry multiple categories. [Cleveland Metroparks' official Big Met page](https://www.clevelandmetroparks.com/golf/courses/big-met-golf-course-course), accessed the same day, identifies one playable Big Met Golf Course at 4811 Valley Parkway and describes parking only as a clubhouse facility.
- Tee Time Spot evidence: production held 31 active `TEST` searches, all waiting for another workflow cycle; the oldest had accumulated 429 probes and several newer rows already had more than 100. A marked signed-out `44113` search returned 20 places and separately offered exact Place ID `ChIJOQuc9RPtMIgR33aMCl2HdBA` (`Big Met Golf Course Parking`) plus the real Big Met course even though both pointed to the same official course page. Current Place Details incorrectly labeled the parking record `primaryType=golf_course`, so structured type data alone could not safely solve this identity.
- Decision: synthetic searches complete after one successful workflow check unless their creation explicitly opts into bounded multi-cycle test mode. Failed checks keep their retry behavior, and public or unclassified searches are unaffected. Separately, add one reviewed `VERIFIED_NON_COURSE` correction for the exact parking Place ID rather than a broad name/type/proximity heuristic.
- Measured search result: the live `44113` corpus changed from 20 to 19 with only `ChIJOQuc9RPtMIgR33aMCl2HdBA` removed, no additions, and every remaining Place ID in the same order. The real Big Met, Little Met, public resort, multi-layout, and verified-public controls remained included. Desktop 1440x1000 and mobile 320x800 screenshots showed no overflow, console/page errors, or same-origin 4xx/5xx responses, and no customer demand was persisted.
- Candidate comparison: the exact parking result was a verified registry-only search correction. The recurring synthetic lifecycle was the stronger systemic reliability/observability issue because it generated continuing provider/workflow load; both were bounded, compatible, and independently verifiable, so both shipped while the category ledger remains balanced away from consecutive search-only work.

## 2026-07-15: Finish the bounded legacy synthetic lifecycle

- Research follow-through: the same-day [Playwright browser-context isolation](https://playwright.dev/docs/browser-contexts) and [OpenTelemetry user-agent semantic attributes](https://opentelemetry.io/docs/specs/semconv/registry/attributes/user-agent/) guidance already established that synthetic state should be isolated and explicitly classified. This run tested whether the shipped one-check decision produced its intended measurable result instead of repeating generic research.
- Tee Time Spot evidence: production dropped from 31 active TEST searches to 10 after the one-check deployment, but the remaining default rows were scheduled as late as July 30. Each had five to seven durable probes, none opted into `syntheticMultiCycle`, and eight retained a legacy `busy` outcome written before the one-check deployment. Three UNCLASSIFIED customer searches remained healthy, with zero actionable real probes, pending alerts, unresolved feedback, or production warning/error/fatal/4xx/5xx logs.
- Decision: apply one bounded cleanup to pre-deployment `TEST`/`AUTOMATION` rows only when they already have probe evidence and did not opt into multi-cycle scheduling. Preserve public and unclassified demand, explicit multi-cycle tests, and synthetic rows without completed evidence. This turns the prior research-backed policy into a complete operational result without adding a recurring broad data mutation.
- Rotation and candidate comparison: signed-out production ZIP `58701` returned Beowulf, Souris Valley, Apple Grove, and Jack Hoeven Wee Links on 1440x1000 desktop and 320x800 mobile. The `AUTOMATION` marker persisted, add/remove ranking worked, all four images loaded, and both viewports had equal document/client widths. A prior `98660` hole-level identity candidate still lacks exact official evidence and representative positive controls, so the measured workflow residue won the non-search reliability/observability comparison.

## 2026-07-15: Keep deterministic browser fixtures off production APIs

- Sources: [Playwright Mock APIs](https://playwright.dev/docs/mock) and [Playwright Network](https://playwright.dev/docs/network), current documentation accessed 2026-07-15 America/New_York, recommend registering `page.route()` handlers before navigation so deterministic API fixtures are fulfilled inside the browser without reaching the target server.
- Tee Time Spot evidence: the broad production log scan since the prior hourly checkpoint found 40 `400` responses and no warnings, errors, fatals, or `5xx` responses. Every `400` was `/api/courses/photo`; a direct-link smoke scenario returned seven synthetic `ui-smoke-photo-*` references but mocked only geocoding and discovery, so its browser requested invalid references from production without failing the test.
- Decision: extract the existing deterministic course-photo route into a focused helper, register it in the direct-link scenario before navigation, and attach the shared browser issue collector so future failed requests make the scenario fail. Keep provider-backed production discovery real in the main journey; only responses derived from synthetic fixture data are intercepted.
- Rotation and candidate comparison: marked signed-out ZIP `64111` returned 18 cross-border Kansas City courses on desktop and 320px mobile. Screenshots, images, add/remove ranking, overflow, console exceptions, and HTTP failures were clean, so no search correction was justified. The due authenticated-dashboard category remains blocked by the absent reserved Clerk identity; removing repeated test-generated production noise was the strongest shippable non-search candidate.
# 2026-07-15 - Managed-challenge CPS booking evidence

- Current evidence: Grassy Hill's stored Google website URL used HTTP and returned `409`, while the same official host over HTTPS exposed its public course and a legacy Club Prophet booking link. That link redirected to `grassyhill.cps.golf`; a normal signed-out browser showed public tee times and prices, while direct server requests returned Cloudflare's explicit managed-challenge response.
- Decision: prefer HTTPS for stale HTTP official-course URLs, try the equivalent secure apex when a stale `www` host fails, and retain a bounded fallback to the original URL. Preserve the direct CPS booking destination, but classify a provider-confirmed managed challenge as `PUBLIC_ONLINE` plus `BLOCKED` / `CAPTCHA_OR_QUEUE`. Tee Time Spot does not replay browser cookies, solve challenges, or call challenge-protected APIs.

# 2026-07-15 - Whoosh direct booking remains available under an automation-policy block

- Sources: [Whoosh End User Terms](https://www.whoosh.io/terms), last updated 2022-04-19 and rechecked 2026-07-15, prohibit using automated agents, robots, crawlers, data-mining tools, or similar mechanisms to search or download Whoosh Platform content. Yale's [2026 course page](https://yalebulldogs.com/sports/2026/2/24/yale-golf-course.aspx) and [FAQ](https://yalebulldogs.com/sports/2026/2/27/faqs.aspx), rechecked 2026-07-15, confirm public online booking through Whoosh.
- Tee Time Spot evidence: Yale's signed-out Whoosh page exposed tee times, capacity, holes, and prices, so `ACCOUNT_REQUIRED` was not an accurate availability classification. The provider-wide terms nevertheless make automated retrieval impermissible even when availability is visible in a normal browser.
- Decision: fetch the current official Whoosh terms when a course links to a Whoosh booking surface. When the terms contain the automated-search prohibition, preserve the official booking URL as `PUBLIC_ONLINE` and classify monitoring as `BLOCKED` / `AUTOMATION_PROHIBITED`. If the terms cannot be verified, retain `NEEDS_REVIEW` rather than assuming either permission or prohibition.

# 2026-07-15 - Reconcile private member amenities already saved as courses

- Sources: Deerwood Country Club's current [membership page](https://www.deerwoodclub.com/membership), [guest policy](https://www.deerwoodclub.com/faq), and [golf program](https://www.deerwoodclub.com/golf-course), accessed 2026-07-15 America/New_York, identify Deerwood as a member-owned private club, limit guests to member-hosted access, and describe The Approach as its six-hole short course for casual play, instruction, and member events. A June 25, 2026 [opening report](https://clubandresortbusiness.com/deerwood-country-club-announces-grand-opening-of-new-short-course-the-approach/) corroborates the exact name and facility identity.
- Tee Time Spot evidence: a marked signed-out production search for Jacksonville ZIP `32256` returned exact Google Place `ChIJAfI2SQDL5YgRBhnU_dStib0` first and labeled it `Public`, with no website, phone, booking URL, or independent public-course identity. The same row had an open engineering-only support incident. Desktop and 320px visual checks showed 33 results without horizontal overflow or saved demand; UNF Golf Complex, Windsor Parke, Casa Linda Oaks, Julington Creek, and the broader Jacksonville corpus remained available as controls.
- Decision: apply one exact `VERIFIED_PRIVATE` review classified `PRIVATE_MEMBER_AMENITY`; do not add a broad short-course, country-club, address, name, membership, or proximity rule. Extend the operator reconciliation seam so a private review updates an already-persisted `Course` and resolves its incident, rather than affecting only future discovery requests.
- Follow-up: the same `32256` corpus includes individual TPC Sawgrass tee and green pins. Preserve that as a separate search/discovery candidate requiring exact IDs, official identity evidence, full before/after accounting, and multi-market playable-course controls; do not piggyback it onto this incident batch.

# 2026-07-15 - GolfBack exposes a bounded public availability seam

- Current primary sources (accessed 2026-07-15; no page-level update date shown): `https://windsorparke.com/`, `https://windsorparke.com/tee-time-policies/`, and `https://api.golfback.com/api/v1/courses/5a90fb0c-b928-43f0-9486-d5d43c03d25d`.
- Evidence: Windsor Parke's official site and tee-time policy advertise public online booking, real-time availability, and dynamic pricing through `https://golfback.com/#/course/5a90fb0c-b928-43f0-9486-d5d43c03d25d`.
- Provider behavior: a generic headless browser received an Azure Front Door response, but Tee Time Spot's declared fetch identity received the public course configuration and read-only tee-time response without login, cookies, or a booking session. Windsor Parke reports an eight-day booking window and the public tee sheet returned source IDs, course-local times, player capacity, holes, and prices.
- Decision: add a reusable GolfBack adapter that reads only the public course and tee-time endpoints, advertises the declared Tee Time Spot identity, and preserves the official GolfBack page for the golfer. It never calls lock, cart, checkout, payment, account, or reservation endpoints.

# 2026-07-15 - Preserve official Chronogolf widget identity before page copy

- Current primary sources (accessed 2026-07-15 America/New_York; no page-level update date shown): [Hyde Park Golf Club](https://hydepark.golf/) identifies Jacksonville's oldest public course and embeds Chronogolf club ID `4006`; its [public Chronogolf profile](https://www.chronogolf.com/club/hyde-park-golf-club) exposes online booking and one course UUID; the existing read-only marketplace endpoint returns signed-out availability without entering reservation or checkout.
- Tee Time Spot evidence: the official-site collector recognized the Chronogolf widget script but discarded the inline `window.chronogolfSettings` block. Hyde Park's long marketing page also filled the evidence limit before provider configuration, leaving the course `UNKNOWN` through five incident occurrences even though the existing Chronogolf adapter supported the provider.
- Decision: recognize the exact Chronogolf settings object in both HTTP and browser evidence collectors, and prioritize bounded machine-readable provider configuration before body copy. Keep the existing public marketplace adapter and official booking handoff unchanged; do not read reservation, account, payment, or checkout surfaces.
- Measured result: live discovery learned club `4006` and course `5d8f129e-e7b8-4855-970c-bd3bc39879a3`. The focused synthetic search observed 66 player-eligible slots, matched 20 within its requested window, and left booking on the canonical Chronogolf profile.

# 2026-07-22 - Keep an indoor Trackman facility out of public-course discovery

- Current primary source: [The Creeks Golf & RV Resort](https://thecreeksgolf.com/), accessed 2026-07-22 America/New_York, identifies The 19th Hole as its climate-controlled Trackman simulator and practice facility while separately describing the physical 18-hole Creeks Golf Course.
- Tee Time Spot evidence: a marked production rotation for ZIP `72761` returned exact Google Place `ChIJM7da6psNyYcRZxvbI4C3ihM` as a second public course beside the real Creeks Golf Course at the same address and phone number. The correction uses only that stable Place ID; Siloam Springs Golf Club, Creeks Golf Course, and Brush Creek Golf Course are the same-market positive controls.
- Decision and measured result: apply one active `VERIFIED_NON_COURSE` review classified `INDOOR_SIMULATOR`. The same live 15-mile discovery changed from four results to three, removed only The 19th Hole listing, and preserved all three physical-course controls. No name, address, proximity, simulator, practice-facility, or colocated-business heuristic was added.
- Candidate comparison: a fresh production mobile Lighthouse sample for `/search` scored 73 performance with a 6.3-second decorative-image LCP, while accessibility, best practices, and SEO scored 100. The image already loaded eagerly at high priority and spent only about 201 milliseconds in resource delay plus transfer; the measured delay was mostly after loading, so a preload-only change was rejected as non-causal. Production had no warning, error, fatal, or `5xx` logs in the sampled six-hour window, zero pending alerts, zero unresolved feedback, and `npm audit` reported zero vulnerabilities. The exact reproducible false-course result therefore outranked speculative performance churn and the blocked authenticated-Discord coverage gap.

# 2026-07-23 - Recover a public course from an ambiguous primary type

- Current primary sources: [Wheeler Family Traditions](https://wheelertraditions.com/), accessed 2026-07-23 America/New_York, identifies a public 18-hole golf course at 37 Harrison Road in Wallingford and links directly to its tee-time page. Google's current [Place Types (New)](https://developers.google.com/maps/documentation/places/web-service/place-types) and [Text Search (New)](https://developers.google.com/maps/documentation/places/web-service/text-search) documentation, accessed the same day, explain that a place can have several descriptive types but only one primary type, and that strict type filtering can still return a place matching the included type.
- Tee Time Spot evidence: real `BROKEN` feedback recorded a lookup miss for `Wheeler Family Traditions` near ZIP `06524`. The exact production lookup returned zero matches even though Google's strict `golf_course` text search returned operational Place ID `ChIJ99HILg3O54kRiJLIRU3WbfE`, named `Wheeler Family Traditions Golf Club`, with the official website and address. Google labeled the record `primaryType=sports_club` while also returning `golf_course` in `types`, so the app's conservative generic sports-club exclusion rejected the ambiguous record.
- Decision and measured result: apply one active `VERIFIED_PUBLIC` review classified `PUBLIC_GOLF_COURSE` for the exact stable Place ID and provider coordinates. Do not weaken the reusable sports-club, association, private-club, or non-course filters. The exact live lookup changed from zero results to the single Wheeler course at the official address, and the same 15-mile discovery changed from 17 to 18 by adding Wheeler while retaining Woodhaven, The VUE CT, Highland Greens, Sleeping Giant, Hop Brook, East Mountain, Oak Lane, Yale, and the remaining prior results.
- Candidate comparison: the current run found zero pending alerts, zero due course-support incidents, no production error, fatal, or `5xx` logs, and zero production dependency advisories. Local mixed desktop/mobile smoke passed 84/84. A fresh production mobile Lighthouse sample scored 76 performance with 6.28-second LCP and 100 for accessibility, best practices, and SEO; the slow LCP remains a separate trace-level candidate because this exact customer miss had direct real-user evidence, a stable-ID fix, and cheaper end-to-end proof.

# 2026-07-23 - Refresh the oldest stale public Course Guides

- Current primary sources: [Orchards Golf Course](https://orchardsgc.com/), the Town of Stratford's [Short Beach Golf Course](https://www.stratfordct.gov/page/short-beach-golf-course) and [facility page](https://www.stratfordct.gov/o/stratford/page/facility-administration), and The Tradition at Oak Lane's [official home](https://www.traditionatoaklane.com/) and [course page](https://www.traditionatoaklane.com/course), accessed 2026-07-23 America/New_York.
- Evidence: the official pages currently support Orchards as a public nine-hole Milford course with ForeUP and phone reservations, Short Beach as a seasonal municipal par-three facility with six-day reservations and carts, and The Tradition as a public 18-hole, par-72 daily-fee course with range, bunker, and on-site dining amenities.
- Decision: refresh the three oldest actionable stale profiles as one metadata batch, preserve facility-focused public prose, and add only structured layout/par facts directly supported by the official sources. Keep provider access classifications and alert scheduling unchanged.
- Measured result: all three drafts passed the protected profile validator before production apply; the three canonical guides returned to published current state, and Orchards gained verified nine-hole evidence while The Tradition retained verified 18-hole and par-72 evidence.
